# STM32H7S78-DK 開發筆記

這份記錄的是**做這塊板子時實際踩到的坑**，以及花時間才查出來的正確做法。
之後在這塊板子上做別的專案，先讀這份可以省下大量除錯時間。

每一條都標註了「症狀 → 原因 → 解法」，因為症狀往往跟原因看起來毫無關聯。

---

## 板子基本規格

| 項目 | 規格 |
|---|---|
| MCU | STM32H7S7L8H6H，Cortex-M7 @ 600 MHz |
| **內部 Flash** | **只有 64 KB** ← 這點決定了整個架構 |
| 外部 Flash | 128 MB Octo-SPI NOR（Macronix MX66UW1G45G），映射在 `0x70000000` |
| 外部 RAM | 32 MB PSRAM（AP Memory APS256XX），映射在 `0x90000000` |
| LCD | 5 吋 800×480 RGB565，RK050HR18 面板（MB1860 子板） |
| 觸控 | GT911 電容式五點觸控，I2C1，位址 `0xBA` |
| 除錯器 | 板載 ST-Link V3（`VID_0483 / PID_3754`） |

**內部 Flash 只有 64 KB** 是這塊板子最特別的地方。一個 800×480 RGB565 的
framebuffer 就要 750 KB，程式碼根本放不進內部 Flash。所以必須用 XIP 架構：
內部 Flash 放 bootloader，程式本體燒進外部 Flash 直接執行。

---

## 一、專案建置

### 1.1 必須用 Template_XIP 當基底

ST 韌體包裡 `Projects/STM32H7S78-DK/Templates/Template_XIP` 已經處理好
bootloader、XIP 鏈結腳本、XSPI 記憶體映射。不要從零開始搭。

### 1.2 專案目錄不能隨便搬

**症狀**：編譯報 `No rule to make target 'C:/Middlewares/ST/...'`

**原因**：`.cproject` 用相對路徑 `../../../../../../..` 指向韌體包的
`Drivers/` 和 `Middlewares/`。把專案複製到別的地方，往上爬的層數就錯了，
會爬到磁碟根目錄。

**解法**：專案必須放在 `cube/Projects/STM32H7S78-DK/Templates/<你的專案>`
這個層級。

### 1.3 submodule 一定要抓

**症狀**：缺 `stm32_boot_xip.c`、`stm32h7rsxx_hal_ltdc.c` 等檔案

**原因**：`STM32CubeH7RS` 倉庫把 HAL、CMSIS、BSP、GT911 都做成 submodule，
`git clone --depth 1` 不會帶下來。

**解法**：
```bash
git submodule update --init --depth 1 --recursive
```

### 1.4 元件設定檔要自己補

Template_XIP 不含 `gt911_conf.h`、`mx66uw1g45g_conf.h`、`aps256xx_conf.h`，
但 BSP 需要它們。從 `Projects/STM32H7S78-DK/Examples/BSP/Inc/` 複製過來。

### 1.5 HAL 模組預設全是關的

`stm32h7rsxx_hal_conf.h` 裡 LTDC / I2C / DMA2D / XSPI / LPTIM 預設都被註解掉。
少開一個就是一串 `unknown type name` 錯誤。

LCD 背光用 LPTIM，所以連 LPTIM 都要開——這個很容易漏。

### 1.6 命令列編譯

CubeIDE 可以不開 GUI 編譯，但有兩個雷：

```bash
# 路徑必須用 Windows 反斜線格式，否則 Eclipse 會把 C: 當成 URI scheme
# 報錯 "No file system is defined for scheme: C"
stm32cubeidec.exe -nosplash \
  -application org.eclipse.cdt.managedbuilder.core.headlessbuild \
  -data 'C:\path\to\ws' -import 'C:\path\to\project' \
  -build 'ProjectName/Debug'
```

`-import` 和 `-build` 要在**同一次呼叫**裡，分兩次執行工作區狀態不會保留。

### 1.7 燒錄

```bash
# Bootloader 進內部 Flash（只需第一次）
STM32_Programmer_CLI -c port=SWD mode=UR -w Binary/Boot_XIP.hex -v

# 應用程式進外部 Flash，必須指定 external loader
STM32_Programmer_CLI -c port=SWD mode=UR \
  -el ".../ExternalLoader/MX66UW1G45G_STM32H7S78-DK.stldr" \
  -w Appli/Debug/App.elf -v
```

漏掉 `-el` 就燒不進外部 Flash。

**硬體前提**：SW1（BOOT0 滑動開關）要在 **0** 的位置。Option Bytes 需要
`XSPI1_HSLV=1`、`XSPI2_HSLV=1`、`VDDIO_HSLV=0`（出廠通常已正確，可用
`-ob displ` 確認）。

---

## 二、顯示（LTDC）── 這裡的坑最多

### 2.1 BSP_LCD_Init 預設是 ARGB8888，不是 RGB565

**症狀**：畫面左右重複兩次、下半部是彩色雜訊、顏色完全不對

**原因**：`BSP_LCD_Init()` 內部呼叫
`BSP_LCD_InitEx(..., LCD_PIXEL_FORMAT_RGB888, ...)`，而 BSP 把 RGB888 實作成
**ARGB8888（4 bytes/pixel）**。如果你的繪圖是 RGB565（2 bytes/pixel），
硬體讀取的行距就是你的兩倍，畫面自然錯亂。

**解法**：明確指定像素格式。
```c
BSP_LCD_InitEx(0, LCD_ORIENTATION_LANDSCAPE,
               LCD_PIXEL_FORMAT_RGB565, 800, 480);
```

### 2.2 換 framebuffer 千萬不要用 BSP_LCD_SetLayerAddress

**症狀**：畫面持續閃爍、出現橫向黑線

**原因**：`BSP_LCD_SetLayerAddress()` → `HAL_LTDC_SetAddress()` →
`LTDC_SetConfig()`，而 `LTDC_SetConfig` 為了換一個位址，**重寫整組圖層暫存器**：
視窗座標、像素格式、混色係數、行長、行數……而且好幾個是這種寫法：

```c
LTDC_LAYER(...)->CFBLR &= ~(mask);   /* 先清成 0 */
LTDC_LAYER(...)->CFBLR  = (value);   /* 再寫回 */
```

兩步之間暫存器是 0。面板持續掃描，掃到那一瞬間就是黑線。

**解法**：只寫 `CFBAR` 一個暫存器。它是 shadow register，寫入不會立即生效，
等垂直消隱重載時整批原子切換。

```c
LTDC_Layer1->CFBAR = new_framebuffer_addr;   /* 單次寫入，無中間狀態 */
LTDC->SRCR = LTDC_SRCR_VBR;                  /* 垂直消隱時生效 */
while (LTDC->SRCR & LTDC_SRCR_VBR) { }       /* 等它真的切過去 */
```

**這是本專案最重要的一條**，任何用 LTDC 做雙緩衝的專案都適用。

初始化時記得先讓 BSP 進入 no-reload 模式，否則 `SetLayerAddress` 仍會走
立即重載那條路：
```c
BSP_LCD_Reload(0, BSP_LCD_RELOAD_NONE);
```

### 2.3 雙緩衝的索引很容易寫反

**症狀**：畫面閃爍，但暫停（畫面靜止）時不閃

**原因**：交換後把繪圖目標指到「正在顯示」的那塊緩衝區，等於雙緩衝失效，
每格都在使用者眼前重畫。

**判斷技巧**：如果**畫面靜止時不閃、有動畫時才閃**，幾乎可以確定是這個問題，
而不是時序或硬體問題。

**解法**：用紙筆或小程式把狀態機跑幾格，確認「顯示的」和「繪製的」永遠相反。

### 2.4 PSRAM 要設成 write-through

Framebuffer 放在 PSRAM，CPU 寫入會停在 D-Cache，而 LTDC 是直接從 PSRAM 讀，
兩邊看到的內容會不一致。

```c
MPU_Region_InitTypeDef mpu = {0};
mpu.BaseAddress  = 0x90000000;
mpu.Size         = MPU_REGION_SIZE_4MB;
mpu.IsCacheable  = MPU_ACCESS_CACHEABLE;
mpu.IsBufferable = MPU_ACCESS_NOT_BUFFERABLE;   /* 這組合 = write-through */
mpu.TypeExtField = MPU_TEX_LEVEL0;
```

註：實測這項對**速度**沒有明顯影響，但對**正確性**是必要的。

### 2.5 PSRAM 開機是隨機內容

一定要在顯示開啟前把兩塊 framebuffer 清空，否則第一幀會看到雜訊。

### 2.6 直立顯示要自己做旋轉

BSP 只支援 `LCD_ORIENTATION_LANDSCAPE`，沒有 portrait 選項。
要做直立畫面就得在繪圖層自己轉座標：

```c
/* 邏輯直立 (x,y) -> 實體橫向 (y, GFX_W-1-x) */
offset = (GFX_W - 1 - x) * PHYS_W + y;
```

觸控讀進來要做**反向**轉換，兩者必須互為逆運算，否則手指按的位置和畫面上
按鍵的位置對不上。這點務必寫測試驗證。

---

## 三、DMA2D ── 已解決

### 3.1 根本原因：CPU 讀取時的快取一致性

**症狀**：畫面出現大範圍白色雜點（偶發、一整片同時出現），控制區出現遊戲
場地的顏色碎片。

**原因**：DMA2D 繞過 CPU 直接寫 PSRAM，但 **CPU 讀取時仍會從 D-Cache 取值**，
拿到的是 DMA2D 寫入之前的舊內容。

這裡最容易誤判：framebuffer 已經設成 **write-through**，所以我一開始排除了
快取的可能。但 write-through 只保證「CPU 的**寫入**」會穿透到 PSRAM，
**完全管不到「CPU 的讀取」這個方向**。DMA 寫、CPU 讀，仍然需要手動失效快取。

**解法**：讀取被 DMA2D 寫過的區域之前，先失效該範圍的快取。

```c
SCB_InvalidateDCache_by_Addr((uint32_t *)addr, (int32_t)byte_len);
```

### 3.2 診斷方法（值得複製到其他專案）

雜點是偶發的，在遊戲裡很難定位。做法是把 DMA2D 單獨拉出來寫成診斷程式
（`app_src/dma2d_probe.c`），用固定樣式反覆填色再逐一驗證，把「偶發」變成
「可量測的錯誤率」，並在出錯當下記錄硬體暫存器狀態。

四個階段，逐步加壓：
1. 單次大面積填色 ×200
2. 200 格小矩形 ×100 回合（模擬遊戲用法）
3. DMA2D 與 CPU 交錯寫入相鄰區域 ×200
4. 連續發送不逐次等待 ×100

結果寫進全域變數，用 SWD 讀出來，不需要 UART：
```bash
addr=$(arm-none-eabi-nm app.elf | grep " probe_errors$" | awk '{print "0x"$1}')
STM32_Programmer_CLI -c port=SWD mode=HOTPLUG -r32 $addr 1
```

**關鍵線索**：錯誤像素的實際值是「上一回合的顏色」而不是隨機值 → 說明不是
寫錯資料，是**讀到了舊內容**。同時硬體狀態顯示 TC 已置位、START=0、傳輸次數
完全正確 → 傳輸本身沒問題。兩者一對照，就只剩快取這一個可能。

### 3.3 其他必須做對的事

**不要用 HAL_DMA2D_Start + HAL_DMA2D_PollForTransfer**：
`PollForTransfer` 的等待迴圈包在 `if (CR & DMA2D_CR_START)` 裡。小矩形傳輸
很快，常在程式走到 poll 之前就結束，START 已被硬體清 0，整個等待被跳過。
而 `HAL_DMA2D_Start` 又從不清除 TC 旗標，殘留的 TC=1 會讓下一次等待立刻返回。

直接操作暫存器比較可靠（見 `app_src/dma2d.c`）：
```c
DMA2D->OCOLR = color;        /* R2M + RGB565 輸出時直接放 16 位元值 */
DMA2D->OMAR  = dest_addr;
DMA2D->OOR   = stride - width;
DMA2D->NLR   = (width << DMA2D_NLR_PL_Pos) | height;
DMA2D->IFCR  = DMA2D_IFCR_CTCIF | DMA2D_IFCR_CTEIF | DMA2D_IFCR_CCEIF;
DMA2D->CR   |= DMA2D_CR_START;

while (DMA2D->CR & DMA2D_CR_START) { }   /* 看 START 位，不要看旗標 */
__DSB();
```

**注意 OCOLR 的格式取決於你怎麼呼叫**：
- 直接寫暫存器：依輸出格式，RGB565 就放 16 位元值
- 走 `HAL_DMA2D_Start`：**一律要 ARGB8888**，HAL 自己narrow。傳 RGB565
  進去會讓紅色通道整個變 0，畫面發灰

**小矩形用 CPU 反而快**：DMA2D 每次要設四個暫存器再啟動，幾百個像素的矩形
設定成本大於傳輸本身。實測門檻約在 1024 像素。

### 3.4 混用硬體與 CPU 繪圖時必須同步

**症狀**：某個區域的小圖案不完整而且會閃，但其他區域正常。

**原因**：填色是非同步的（發出傳輸後函式就返回）。若隨後用 CPU 在同一區域
畫小東西，DMA2D 正在進行的傳輸會把剛畫好的內容蓋掉。

這個 bug 只在**同一塊區域混用兩種路徑**時出現。俄羅斯方塊的預覽框正是這樣：
框底 190x68 走硬體，裡面的方塊 14x14 走 CPU，於是方塊被框底抹掉。

**解法**：在繪圖層設一個同步點，任何 CPU 寫入 framebuffer 之前先等硬體停下來。

```c
/* gfx.c */
static gfx_sync_hw_fn g_sync_hw;
static inline void sync_hw(void) { if (g_sync_hw) g_sync_hw(); }

/* 韌體初始化時 */
gfx_set_fill_hw(dma2d_fill);
gfx_set_sync_hw(dma2d_wait);
```

**同步點的粒度很重要**：放在 `gfx_pixel` 會讓畫一行字多做上千次檢查。
應該放在 `gfx_char`、`gfx_circle` 這種「一次寫幾百個像素」的函式開頭。

之後改用 GPU2D 也是同一個模式，這個 callback 機制可以直接沿用。

### 3.5 效能

| | 繪製 | 等待垂直消隱 | fps |
|---|---|---|---|
| 純 CPU | 9.87 ms | 5.71 ms | 64 |
| DMA2D（門檻 1024px） | 8.94 ms | 6.63 ms | 64 |

幀率相同，因為瓶頸是面板的 60 Hz。DMA2D 的價值在於**騰出 CPU 時間**，
以及未來做全畫面重繪（賽車類）時才會顯現。

### 3.6 這顆還有更強的 GPU2D

`GPU2D_BASE` 存在，HAL 有 `stm32h7rsxx_hal_gpu2d.c`。那是 NeoChrom GPU，
支援旋轉、縮放、紋理貼圖、抗鋸齒——DMA2D 只能做矩形搬運與混色。
做賽車這類需要旋轉縮放的遊戲時應該用它。

## 四、效能實測數據

用 DWT cycle counter 在板子上實際量測（600 MHz）：

| 項目 | CPU 繪圖 | DMA2D（門檻 1024px） |
|---|---|---|
| 每格繪製 | 9.87 ms | 8.94 ms |
| 等待垂直消隱 | 5.71 ms | 6.63 ms |
| 合計 | 15.58 ms | 15.57 ms |
| 幀率 | 64 fps | 64 fps |

**結論**：兩者幀率相同，因為瓶頸是面板的 60 Hz 更新率，不是繪製速度。
單純的 2D 遊戲用 CPU 畫就夠了；DMA2D 的價值要在全畫面重繪時才顯現。

註：目前遊戲的方塊是 24×24 = 576 像素，低於 1024 的門檻，所以場地其實
還是走 CPU。若把門檻調低反而更慢（設定成本大於傳輸）。真正要發揮 DMA2D，
應該改成「一次傳輸畫一大塊」而不是「每個小方塊一次」。

量測方法：
```c
CoreDebug->DEMCR |= CoreDebug_DEMCR_TRCENA_Msk;
DWT->CYCCNT = 0;
DWT->CTRL |= DWT_CTRL_CYCCNTENA_Msk;
uint32_t t0 = DWT->CYCCNT;
/* ... 要量的程式 ... */
uint32_t cycles = DWT->CYCCNT - t0;
```

之後用 `STM32_Programmer_CLI -c port=SWD mode=HOTPLUG -r32 <變數位址> 1`
就能把數值讀出來，不需要接 UART。變數位址用
`arm-none-eabi-nm app.elf | grep 變數名` 查。

---

## 五、資源用量（決定能塞多少遊戲）

| 項目 | 大小 |
|---|---|
| 遊戲本體（邏輯+繪圖+UI+輸入+字型） | 16.4 KB Flash / 296 B RAM |
| 其中中文字型（74 字，可共用） | 5.6 KB |
| 扣掉字型的純遊戲 | 10.8 KB |
| HAL + BSP 基礎建設（所有遊戲共用） | 約 145 KB |
| Framebuffer ×2（所有遊戲共用） | 1.5 MB PSRAM |

外部 Flash 有 **128 MB**，目前只用了 60 KB。
純程式類的 2D 遊戲每款約 10~20 KB，**容量完全不是限制**。

---

## 六、中文字型

BSP 內建字型只有 ASCII。中文要自己做點陣字：

`tools/genfont.py` 用 Pillow 從微軟正黑體算出點陣圖，產生 C 陣列。
24×24 每字 72 bytes，74 個字只要 5.2 KB。

搭配 `tools/checkglyphs.py` 在建置前檢查所有 UI 字串用到的字都在字型表裡——
**漏字在畫面上是「什麼都不顯示」，不會報錯**，所以這個檢查很有必要。

---

## 七、測試策略

沒有板子也能測大部分東西：用 **QEMU 的 `mps2-an500`**（Cortex-M7，跟這塊板子
同核心），配合 semihosting 輸出。

```bash
qemu-system-arm -M mps2-an500 -nographic \
  -semihosting-config enable=on,target=native -kernel test.elf
```

需要 `-specs=rdimon.specs`，並在啟動碼裡呼叫 `initialise_monitor_handles()`
（用 `-nostartfiles` 時 crt0 不會自動呼叫它，輸出會是空的）。

可以測到的：遊戲邏輯、座標旋轉、字型資料、記憶體越界、觸控映射。
測不到的：真實顏色、畫面方向、觸控準度、閃爍。這些只能燒上板用眼睛看。

**教訓**：純數學測試驗證了「旋轉映射是雙射」，卻沒發現**畫面整個上下顛倒**——
雙射只保證一對一，不保證方向正確。所以務必把畫面**渲染成 PNG 檢查**
（`test/render_shot.c` + `tools/raw2png.py`），視覺問題只有看才看得出來。

---

## 八、通用教訓

1. **症狀的位置就是線索**。「只有 A 鈕那側閃」讓我算出是 6 px 的重繪縫隙；
   「暫停時不閃」直接證明問題在動畫而非硬體時序。使用者的精確描述比我的推理有用。

2. **二分法比推理快**。花了三輪猜 DMA2D 的問題出在哪，最後直接把它關掉，
   一次就確定了範圍。

3. **量測，不要假設**。我先後假設「繪製太慢」「快取沒寫回」，實測後都被推翻。
   DWT cycle counter 花五分鐘裝上去，省下數小時瞎猜。

4. **測試要先驗證它會失敗**。寫完閃爍偵測測試後，我故意把 bug 改回去確認測試
   真的會 FAIL——第一版測試其實是無效的，改回去也照樣 PASS。

5. **功能完成之後，改動的動力要來自實際的不順，不是來自待辦清單。**
   這輪結尾列了四項待辦，接手的對話很自然地從第一項開始規劃，審查的一方
   也跟著往下審 —— 整串沒有人問過「這個真的該做嗎」，直到使用者問
   「功能不是都有了，為何還要繼續改東西」。

   那一項（照片解碼改 DMA）算起來是：使用者感覺不到的收益，換動到正式路徑
   的風險，而正式路徑正是先前被弄壞三次的那條。**沒有人抱怨的問題，
   不值得動正式路徑去修。**

   清單記的是「當時想到還可以做什麼」，是選項不是佇列。
   審查任何計畫的第一句話應該是「這個真的該做嗎」，而不是「這樣做對不對」。

6. **症狀是「時好時壞」時，先懷疑資料放在哪裡，不要一直改邏輯。**
   隨身碟模式的自動切換卡了整晚：拔線有時回得來有時不行、按 reset 有時
   有效有時無效。我一路在「事件選錯了嗎／條件寫錯了嗎／快取沒寫回嗎」
   之間打轉，每一輪都要使用者插拔一次來驗證。

   真正的原因是那個跨 app 傳遞的暗號被放在 `0x24071BF0`，而 bootloader 的
   堆疊指標是 `0x24072000` —— 只隔 1040 個位元組，bootloader 一跑就踩掉了。
   **邏輯從頭到尾都是對的，錯的是資料的存放位置。**

   而這件事**三十秒就能自己驗出來**：寫一個特徵值進去、重置、讀回來。
   不需要任何人插拔，也不需要猜。我卻先花了好幾個小時在正確的邏輯上找錯誤。

   間歇性 = 有東西在競爭。競爭的可能是時序，**也可能只是位址**。

---

## 九、SWD 除錯的連線模式（另一個專案踩過的坑）

### 9.1 mode=UR 會讓核心永遠停在 reset vector

**症狀**：讀 RAM marker 永遠是 0，看起來像「main() 根本沒跑」「iRoT 擋住 boot」。

**原因**：`mode=UR`（Connect Under Reset）每次連線都 halt-on-reset。
而 CLI 的每個指令是**獨立連線** —— 你 `-g` run 之後，下一個讀取指令重新連線
又把核心 halt 回 reset 點，marker 永遠沒機會被寫。

```bash
# ❌ 跨連線讀取，每次都把核心 halt 掉
STM32_Programmer_CLI -c port=SWD mode=UR -r32 0x24000100 1

# ✅ HOTPLUG 不 halt 執行中的核心
STM32_Programmer_CLI -c port=SWD mode=HOTPLUG -r32 0x24000100 1

# ✅ 或單一連線內做完所有動作
STM32_Programmer_CLI -c port=SWD mode=0 -hardRst -r32 0x24000100 4
```

**燒錄用 UR、觀察執行狀態用 HOTPLUG**，不要混用。

> 這個坑讓另一個專案誤判成「iRoT secure boot 擋住未簽名映像」，
> 花了整整一個 session 追不存在的問題。實際上 chainload 一直是通的。

### 9.2 ST-Link VCP 是 UART4，不是 USART2

從 Nucleo 等其他板子抄來的 `USART2 / PA2-PA3` 在這塊板子行不通。

| | 值 |
|---|---|
| UART | **UART4** |
| TX / RX | **PD1 / PD0** |
| AF | **8** |
| Baud | 115200 8N1 |

Boot 跟 Appli **兩個 context** 都要設（hal_conf 啟用模組、hal_msp 加 MspInit、
main.c 加 huart4 + `__io_putchar`）。

### 9.3 GPIO 在 AHB4：0x58020000

H7RS 系列跟傳統 H7 不同。

```c
#define GPIOD_BASE_ADDR 0x48020C00   // ❌ 無效映射 → BusFault → LOCKUP
#define GPIOD_BASE_ADDR 0x58020C00   // ✅ AHB4PERIPH_BASE + 0x0C00
```

症狀是 CFSR=0x8200、HFSR LOCKUP。寫 raw 暫存器（不透過 HAL）時特別容易踩。

### 9.4 reset 直後的時鐘是 HSI 16MHz

還沒跑 `SystemClock_Config()` 之前是 **HSI 16MHz**，不是 64MHz。
早期 raw UART print 的 BRR 要照 16MHz 算，否則會變成約 461k baud，終端機讀不到。

### 9.5 AFR 的位元組位置

```c
/* pin N 的 AF 欄位在 AFR[N/8] 的 bits[(N%8)*4 +: 4] */
GPIOD->AFR[0] = (8 << 4);    /* PD1 = AF8 */
```

寫錯位移會設到別的 pin，症狀是「UART 初始化了但沒輸出」。


---

## 十、同一份韌體換一塊板子就閃 ── PSRAM 時脈邊際

### 10.1 症狀

畫面上有很多細線在閃，**暫停讓畫面完全靜止也照閃**。
同一顆 bin 在原本那塊板子上完全正常。

第二節列的五個閃爍成因（`SetLayerAddress`、雙緩衝索引寫反、write-through、
DMA2D 快取、硬體/CPU 混用）在這個情況下全都不適用——那些都是程式問題，
程式沒變卻換板子就發作，就要往硬體時序想。

### 10.2 根因

framebuffer 在外部 PSRAM，BSP 把 XSPI1 設在這顆晶片的規格上限 **200 MHz**
（`stm32h7s78_discovery_xspi.c` 的 `BSP_XSPI_RAM_Init`，`xspi_max_freq`）。
時序餘裕會隨 PCB 走線、晶片批次、溫度、供電而變，**不同板子可能一塊在邊界的
安全側、一塊在錯誤側**。落在錯誤側時讀取會偶發單一位元取樣錯誤。

關鍵是「誰在讀」：

| | 受影響程度 |
|---|---|
| CPU 讀 framebuffer | 幾乎不受影響——走 D-Cache，而且遊戲很少讀回畫面 |
| **LTDC** | **每秒 60 次、每個像素都直接從 PSRAM 讀** |

每個讀錯的位元就是螢幕上一個變色像素。錯誤是隨機的，所以**每一幀壞掉的位置
都不同**，那些像素就以刷新率在閃。LTDC 沿掃描線 burst 讀取，錯誤會沿同一列
聚集，看起來就是「很多線條在閃」而不是散開的雜點。

### 10.3 解法：把 XSPI1 時脈減半

`MX_XSPI_RAM_Init()` 是 `__weak`，在應用層定義同名函式就能覆寫，
**不必改韌體包裡的任何檔案**（重跑 `setup.sh` 也不會被蓋掉）。

複製 BSP 版本的內容，只改兩處：

```c
/* 分頻 = prescaler + 1，所以這樣寫等於時脈減半：200MHz -> 100MHz */
hxspi->Init.ClockPrescaler = (Init->ClockPrescaler + 1U) * 2U - 1U;

/* refresh 是「2us 換算成幾個時脈」，時脈變了一定要跟著重算，
   否則會違反 PSRAM 的 tCEM 上限，反而更糟 */
hxspi->Init.Refresh =
    ((2U * (hspi_clk / (hxspi->Init.ClockPrescaler + 1U)) / 1000000U) - 4U);
```

只影響 PSRAM（XSPI1）。程式碼在外部 NOR Flash 上（XSPI2），不受影響。

**沒有效能代價**：LTDC 只需要約 46 MB/s（800×480×2 bytes × 60 Hz），
100 MHz 的 PSRAM 頻寬遠遠夠用。實測 fps 沒有下降。

因為這是**板子個體差異**，不是所有板子都需要，所以沒有預設打開。
畫面會閃再加上這段。

### 10.3.1 調取樣點沒有用，只有降頻有用

XSPI 有兩個移動取樣點的旋鈕，直覺上會想先試這個而不是降頻。實測四種組合：

| 設定 | 錯誤字數 | 乾淨區塊 |
|---|---|---|
| 200MHz 取樣 NONE、DHQC 關（BSP 預設）| 1,466,194 | 15/256 |
| 200MHz 取樣半週期、DHQC 關 | 1,357,359 | 19/256 |
| 200MHz 取樣 NONE、DHQC 開 | 1,386,924 | 13/256 |
| 200MHz 取樣半週期、DHQC 開 | 1,414,165 | 4/256 |
| **100MHz 取樣 NONE、DHQC 關** | **0** | **256/256** |

四種組合在 200MHz 全都是一百多萬個錯誤，彼此的差異只是雜訊。
**移動取樣點有明顯改善才代表是相位問題**；這裡沒有，所以是整個資料視窗在
200MHz 下就不夠，屬於訊號品質而不是相位。

另外，prescaler 是整數分頻，核心時脈 200MHz 之下只有 /1=200、/2=100、
/3=66.7 這幾檔，**沒有 150MHz 這個選項**。降到 100MHz 不是保守過頭，
那就是次一檔。

### 10.4 診斷方法（這裡踩了很多坑，值得記下來）

目標是量出「PSRAM 讀回來的資料對不對」，但這件事比想像中難測，
以下每一個都實際誤導過我：

**❌ 用偵錯器讀寫 PSRAM 驗證**
`STM32_Programmer_CLI` 的存取會跟正在全速掃描的 LTDC 搶匯流排，
測到的錯誤可能是競爭而不是記憶體。而且結果不一致——同樣的區域重測三次，
錯誤位址每次都不同，換一塊區域又變成零錯誤。**必須讓韌體自己測。**

**❌ 寫完直接讀回來比對**
framebuffer 區是 write-through，CPU 的**寫入**會穿透到 PSRAM，
但**讀取**仍然可能從 D-Cache 拿到剛剛寫進去的值，等於什麼都沒測到。

**❌ 補一個 `SCB_InvalidateDCache_by_Addr()` 就好**
對 write-through 區域可以，對 **write-back 區域會把還沒寫回去的資料直接丟掉**。
我拿內部 SRAM 當對照組時就是這樣——錯誤率 100%，每個字都錯。

**✅ 測試區設成 non-cacheable，並且一定要有對照組**

```c
mpu.Number       = MPU_REGION_NUMBER6;
mpu.BaseAddress  = 0x91000000;          /* framebuffer 之外的 PSRAM */
mpu.Size         = MPU_REGION_SIZE_16MB;
mpu.IsCacheable  = MPU_ACCESS_NOT_CACHEABLE;
mpu.IsBufferable = MPU_ACCESS_NOT_BUFFERABLE;
mpu.TypeExtField = MPU_TEX_LEVEL1;
```

對照組用**內部 SRAM 跑完全相同的檢測程式碼**。內部 SRAM 不可能出錯，
所以只要對照組不是 0，就代表檢測程式本身有問題，PSRAM 那組數字也不能信。
上面那三個坑，全都是靠對照組爆掉才抓出來的。

**逐塊掃描能分辨兩種完全不同的原因**：

| 錯誤分布 | 代表 |
|---|---|
| 集中在特定區塊 | 實體壞格 → 把 framebuffer 挪到乾淨區域可以繞開 |
| **均勻散布在整片** | **時序/設定問題 → 換位址沒用，要降頻** |

實測結果是後者：16MB 分成 256 塊、每塊跑三輪，
**0 個乾淨區塊**，總錯誤 1,369,266 個字。降頻後 **256/256 全乾淨、錯誤 0**。

### 10.5 兩個假線索

**LTDC 的 `ISR` 旗標是黏著的**。直接讀到 `FUIF|TERRIF`（`0x6`）看起來像
FIFO underrun，但那可能只是開機瞬間發生過一次。要每幀讀完就寫 `ICR` 清掉，
用計數對比幀數才有意義。實測清乾淨之後 underrun 一直是 0，
**這條路是死的**——問題不在頻寬。

**`STM32_Programmer_CLI` 燒錄時的 `Data mismatch` 可能是假警報**。
連續三次在同一位址報同樣的錯，看起來像 Flash 壞格，但重新讀回內容跟映像
完全一致。**要另外讀回來比對才算數**，不要直接相信驗證階段的錯誤訊息。

### 10.6 教訓

**畫面問題先確認是程式還是硬體**：暫停讓畫面靜止還在閃、而且同一份 bin
在另一塊板子正常——這兩點就足以把程式邏輯排除掉。

**同一份韌體換板子就出問題，先降頻試試**。外部記憶體介面的時序餘裕是板子
個體差異最容易出事的地方，降頻是最快的驗證手段，一次就能確認方向對不對，
比任何合成測試都快。

---

## 十一、硬體 JPEG 解碼（album 專案踩的雷，整批都在這）

### 11.1 ChromaSubsampling = 0 是 4:4:4，不是 4:2:0

HAL 的定義：`0=4:4:4、1=4:2:0、2=4:2:2`。把 0 當成 4:2:0 是整個 album
專案最貴的一個誤認 —— 緩衝區照 4:2:0（每像素 1.5 bytes）配，但相機直出的
JPEG 很多是 4:4:4（每像素 3 bytes），需要的空間是兩倍。

| 取樣 | MCU 尺寸 | 每 MCU bytes | 每像素 |
|---|---|---|---|
| 4:4:4 | 8x8 | 192 | 3.0 |
| 4:2:2 | 16x8 | 256 | 2.0 |
| 4:2:0 | 16x16 | 384 | 1.5 |

輸出緩衝區要照 4:4:4 的最壞情況配，容量檢查與長度夾制也要照實際取樣算。

### 11.2 輸出緩衝區不夠時，HAL 會繞回開頭續寫

不會報錯、不會停 —— 就是默默把照片尾端的 MCU 蓋到緩衝區開頭。畫面症狀：
**「照片底部跑到最上面、底部殘留舊資料」**。使用者一說出這句描述，就該
直接朝環繞查，不要繞去查繪圖邏輯（我們繞了四個錯誤假設才回來）。

### 11.3 DataReadyCallback 裡不要呼叫 HAL_JPEG_ConfigOutputBuffer

想在回呼裡「把緩衝區往後推」防覆蓋是個陷阱：HAL 在**解碼結束時也會呼叫
這個回呼**，這時再餵新緩衝區，HAL 就當作還能繼續輸出，反覆觸發直到把
整塊緩衝區吃光。症狀是輸出長度「精確等於緩衝區容量」—— 這種剛好卡在
上限的數字幾乎一定是人為邊界，不是資料本身。

正確做法：一開始就把整塊交給 `HAL_JPEG_Decode()`，回呼只記錄
「這批資料寫到哪裡為止」（用結束位移取最大值，不要累加 —— HAL 對同一批
資料可能回呼兩次）。

### 11.4 HAL_JPEG_Decode 返回後讀不到輸出長度

HAL 結束時先用 DataReadyCallback 把長度交出來，然後把 `JpegOutCount`
清成 0。`__weak` 的預設回呼什麼都不做，長度就掉了 —— 必須自己實作回呼。

### 11.5 解碼器會多寫「MCU 補齊」的部分

高度會補到 MCU 邊界（4:2:0 是 16 的倍數）：1800 高實際寫到 1807 列。
容量檢查照 `寬x高` 算會少算，緩衝區之間也因此**不要首尾相接**，
留一段空白當安全邊界，溢出時不會立刻毀掉隔壁的資料。

### 11.6 jpeg_utils 的 RGB888 是 BGR 順序

`JPEG_RED_OFFSET` 等常數是位元位移不是位元組索引；預設 RED_OFFSET=16，
記憶體順序是 B,G,R。設 `JPEG_SWAP_RB=1` 才是 R,G,B。RGB565 沒這問題
（16 位元值整個寫入），所以從 565 換到 888 時才會突然中招。

### 11.7 縮放的目的地尺寸不要用比例反算

`(H * ((GFX_H<<16)/H)) >> 16` 兩次截斷會讓 800 變 799，最後一列永遠
沒畫到（留著清畫面的黑）。填滿模式的目的地就是整個螢幕，直接寫死，
讓誤差落在來源取樣座標上（那裡差一個像素看不出來）。

## 十二、BSP 的其他不對稱陷阱

### 12.1 BSP_LCD_DisplayOn 不會開背光

`DisplayOff` 順手把背光腳（GPIOG15）從 LPTIM PWM 改成一般輸出並拉低，
`DisplayOn` 卻完全不碰它 —— 關得掉、開不回來。DisplayOn 之後要自己把
GPIOG15 拉高（不用亮度調節的話維持一般輸出即可）。

### 12.2 BSP_SD_IsDetected 只讀不設定偵測腳

真正設定偵測腳的是 `BSP_SD_Init()`。在第一次 Init 之前呼叫 IsDetected，
讀到的是未初始化的浮接腳位 —— 沒插卡會被誤判成有卡。開機要自己先把
`SD_DETECT_PIN` 設成輸入再開始判斷。

### 12.3 觸控要消抖，座標要當下抓

電容觸控在放開瞬間會彈跳：「讀到一次沒碰到」不代表放開了（要連續多次），
「確認有碰到之後再讀座標」會撲空（輕點時手指已離開）。原則：確認的當下
就把座標抓下來；等待放開要連續數次都讀不到才算。

## 十三、建置系統的陷阱

- **產生出來的 makefile 內含絕對路徑**。搬動或改名專案目錄後 make 會
  `No rule to make target`—— 這訊息**不含 error 字樣**，輸出過濾若只抓
  error/warning 會把失敗顯示成成功。把 Debug/ 刪掉重新產生，不要用 sed
  改（`grep -rl` 會把 .o 檔也列進去改壞）。
- **行末的 `\` 會讓整條指令悄悄變成別的東西**。本 repo 的腳本在工作目錄是
  CRLF，行末的 `\` 會被 `` 接走而失去續行作用；而 album 的 `setup.sh` 與
  `build.sh` 裡原本是 **`\`（雙反斜線）**，那連 LF 也不會生效 —— bash 會把它
  當成「一個字面反斜線引數」，下一行變成獨立指令。

  後果非常難查：`build.sh` 的 CubeIDE 指令因此**在沒有 `-build` 參數的情況下
  啟動**，IDE 匯入完專案就一直掛著，不編譯也不結束。症狀是「編譯永遠不會
  回來」，而且看起來像 13.2 那個 workspace 復原掃描，我因此往錯的方向查了
  很久（還去刪 workspace 的 .lock，反而把事情弄得更糟）。

  **教訓：腳本「沒有輸出」跟「卡住」要先確認實際執行的指令長什麼樣。**
  把 grep 拿掉直接看完整輸出，一次就看到 `Project: Album_Appli already exists
  in the workspace!` 與 `Java was started but returned exit code=1`。
  多行指令在 CRLF 的 repo 裡就不要用續行，併成一行最安全。
- **headless 的 workspace 要跟 GUI 的分開**。共用會互相留下 `.lock` 與未存檔
  狀態；而且 `-import` 撞到已存在的專案會直接中止（`already exists in the
  workspace!`），只給 `-build` 又會說 `doesn't appear to be a CDT project`。
  用一個專屬的 workspace 目錄，兩個問題一起消失。
- **CubeIDE headless 被強制中止後**，workspace 留下 .lock 與未儲存狀態，
  下一次啟動會卡在復原掃描很久。能用 make 就用 make。

---

## 十四、省電、按鍵與觸控（album 專案第二輪踩的雷）

### 14.1 __WFI() 會讓 SWD 完全失聯

把輪詢迴圈的 `HAL_Delay()`（忙等）換成 `__WFI()` 睡眠很省電，但核心一睡，
除錯介面的時脈就被關掉 —— SWD 直接報
`Unable to read device id from ROM table`，連讀個變數都做不到。

救援方式：`mode=UR`（連線時按住重置，核心來不及睡）仍然連得上，可以重燒。
根治：開機時呼叫 `HAL_DBGMCU_EnableDBGSleepMode()`。代價是省電效果打折，
但開發階段一定要開。

### 14.2 BSP_LCD_SetBrightness(0) 關不掉背光

公式 `Pulse = (PERIOD+1) * Brightness / 100 - 1`，Brightness=0 時
`0 - 1` 在 uint32 下溢成 `0xFFFFFFFF`，PWM 永遠不翻轉。
0% 要另外處理：把背光腳切成一般 GPIO 輸出並拉低。

回到非 0 時記得把腳位還原成 `GPIO_MODE_AF_PP` + `LCD_LPTIMx_CHANNEL_AF`，
否則 SetBrightness 改了比較值也沒用（腳位已經不歸 LPTIM 管）。

### 14.3 亮度要照感知分檔，不是線性

人眼對亮度接近對數感知，`100/75/50/25` 看起來差別很小。
實用的級距是 `100/50/20/6/0`。

### 14.4 「事件不會漏」和「反應快」是兩件事

按鍵從輪詢改成 EXTI 中斷，解決的是「解碼那 1.5 秒按了會漏掉」；但如果
中斷只立旗標、實際動作留給主迴圈，使用者還是要等 1.5 秒才看到反應。

兩個層次都要處理：
- 能在中斷裡完成的就在中斷裡做（亮度只是寫 GPIO 與 LPTIM 暫存器，很快）
- 需要主迴圈的（暫停、換頁）→ 讓長時間工作**可以中途放棄**：
  在管線各階段之間、以及大迴圈裡每隔幾十列檢查一次旗標，
  最壞反應時間從 1.5 秒壓到幾十毫秒

註：中斷裡不要碰 RCC（時脈開關），先在初始化時開好。

### 14.5 旗標的設定與清除必須對稱

「可中斷」等於引入狀態。加了中斷檢查卻沒想清楚「誰負責清」，就會出現：
暫停中每次觸碰都把旗標立起來 → 按繼續離開時旗標還在 → 下一張一開始解碼
就被判定要放棄 → 立刻又彈回暫停 → **永遠出不去**。
把所有出口收斂到一個 `xxx_exit()` 函式裡統一清除。

### 14.6 BSP_TS_EnableIT() 會讓觸控輪詢失效

啟用觸控中斷後，ISR 只清 EXTI 旗標是不夠的 —— GT911 的資料沒被讀走就
不再回報新觸碰，`BSP_TS_GetState()` 之後永遠回傳「沒碰到」，觸控整個死掉。

要正確使用中斷，回呼裡仍得做一次 I2C 把資料讀走，省不到什麼。
如果只是偶爾要判手勢，**維持輪詢就好**。

而且：**軟體重置不會重置 GT911**，晶片會停在中斷模式，症狀跨重燒持續存在，
必須拔電才會回復（跟 SD 卡那個坑同一個道理）。

### 14.7 快速滑動時觸控會漏報

手指快速移動時控制器會掉幾筆座標。若「一讀不到就結束手勢」，位移只算到
中斷點為止 —— 橫向滑動通常比較快，所以特別容易失效，而慢而長的縱向滑動
卻正常。這個方向性差異本身就是線索。

正確做法：容忍約 150ms 的漏報再判定放開，並取**整段過程中的最大位移**
而不是放開瞬間的位置。

---

## 十五、影像重取樣的畫質（album 專案第三輪）

症狀是「照片邊緣鋸齒太嚴重」。這一章的教訓大多與板子無關，但**方法**值得複製到
任何有畫質問題的專案。

### 15.1 先建 PC 測試台，不要在板子上瞎試

畫質只能用眼睛判斷，而在板子上改一次要重編、重燒、還得手動操作選單才看得到
一張照片，一輪好幾分鐘。`projects/album/test/` 把「解碼之後」的整段管線搬到 PC，
一次跑完幾十種參數組合並排比較。

**關鍵是直接 `#include photo.c` 本體，不要重寫一份演算法。** 重寫一份「應該一樣」
的程式碼來測，測到的就不是板子上實際跑的東西 —— 第七章那個「純數學測試通過、
畫面卻整個上下顛倒」就是這樣來的。

為此把緩衝區位址改成從 `PSRAM_BASE` 算起（`-DPSRAM_BASE=` 可指到 malloc 的記憶體），
把調校常數改成 `#ifndef` 可覆寫。這兩個小改動讓韌體端數值完全不變，卻讓整個
管線變得可在 PC 上參數化測試。

踩過的坑：測試台直接呼叫管線中段，漏了 `photo_init()` 裡的建表，
結果 sRGB 對照表全是 0、反查一律回傳 255、整張變白。
**「輸出全白」「兩個變體位元組完全相同」這種結果要先懷疑旗標沒生效或初始化沒做，
不要急著解讀成演算法的差異。**

### 15.2 整數邊界的盒狀濾波會產生規律階梯

`sy0 = (dy*step)>>16; sy1 = ((dy+1)*step)>>16` 這種寫法，來源像素非全進即全出。
2.25 倍縮放時盒子大小會以 **2,2,2,3 的週期**跳動，取樣中心跟著漂移，邊緣每四個
目的像素就錯開一次 —— 規律的階梯比隨機誤差刺眼得多。而且每個目的像素只由
2~3 顆來源像素平均，灰階過渡只有寥寥幾級，邊緣幾乎是硬跳。

正解是**分數覆蓋率**：邊界上的來源像素按重疊比例加權（`sx1` 改成無條件進位，
權重 = 兩個方向的重疊量相乘）。

### 15.3 重取樣必須在線性光空間做

重取樣的本質是加權平均，而**平均只在線性光空間才有物理意義**。JPEG 解出來是
sRGB（感知編碼，約 gamma 2.2），直接對編碼值平均等於假設亮度尺度是線性的。
誤差集中在高頻，也就是邊緣 —— 正好是鋸齒問題所在。

### 15.4 這兩件事是相乘的，不是相加的

對「線性光 Lanczos」的平均絕對差（越小越好）：

| | sRGB | 線性光 |
|---|---|---|
| 整數盒 | 21.65 | 16.84 |
| 分數覆蓋 | 19.05 | **7.59** |

單獨做任一項只改善 12~22%，兩項一起卻好了 65%。若彼此獨立，預期值是 14.9。

原因：整數盒把邊緣位置量化得太粗，色彩空間的精度根本沒東西可發揮；
幾何修正之後，色彩空間才成為限制因素。**沒有便宜的捷徑，兩個必須一起做。**

這也是一個一般性的陷阱：**逐項比較會低估組合效果**。我一開始只比了
「分數覆蓋 vs 原版」（12%）就下結論說主要貢獻來自線性光，那是比較順序造成的假象。

### 15.5 銳化不能取代好的濾波器

文獻明確反對縮圖後補銳化（會在前景物體產生 ringing）。實測也一致：
銳化強度 0/30/60/100 對應的偏差是 5.63/5.85/6.61/7.59，越銳化離理想越遠。
但這只說明「銳化不是抗鋸齒的解法」，不代表要關掉 —— 那是感知取捨，
合成測試圖（全是硬邊黑白）是銳化表現最差的場景，真實照片有紋理，價值在別處。

### 15.6 Lanczos 不是「最好」的選擇

Lanczos 靠負瓣換銳利度，代價是硬邊緣會 ringing。要「平滑」的話負瓣是扣分項。
而且 2.25 倍縮放下 Lanczos-3 每個目的像素要取樣約 14x14 個來源像素，
是面積平均的 20 倍。面積平均沒有負瓣，不可能 ringing。

### 15.7 分數覆蓋的成本來自「盒子重疊」，不是權重運算

改完之後縮放從 241 ms 變成 906 ms（3.75 倍）。我原本估「幾乎不花時間」，
理由是這段被匯流排卡住 —— 錯了，如果真的是匯流排受限，加算術不會變成 3.75 倍。

真正的原因是 `sx1` 改成無條件進位之後**相鄰盒子會重疊一個像素**：
2.25 倍下每個盒子從 2~3 格變成 3~4 格，走訪次數約 2.1 倍，再乘上每個樣本
多出的乘法與查表。

兩個優化把它救回一半（906 → 528 ms，畫質數值完全不變）：

- **反向 gamma 用查表，不要二分搜尋。** 原本每個目的像素三通道各八次帶分支
  迭代，480x800 就是九百多萬次難預測的分支。改成 4096 格的反查表（4KB，
  塞得進 D-Cache）。格子粒度 16，而最暗處相鄰 sRGB 階的線性差距約 20，
  暗部不會併階。
- **水平權重預先建表。** 同一組權重被 800 個目的列重複使用，原本每個來源樣本
  都現算一次 min/max/減法/移位。存成緊湊格式（偏移＋長度）而不是固定跨距 ——
  實際摸到的資料只有約 4KB，留得住快取；固定跨距要 19KB 就放不下。

### 15.8 隨機播放讓兩次執行不能直接比

相簿是隨機播放，每次抽到的照片尺寸分布都不同。連續三輪比較都得拿
「JPEG 解碼時間」當尺寸代理去估算，估出來的倍率從 1.7 到 2.4 都有。

**量測有樣本變異的系統時，一定要同時記錄一個規模指標。** 這裡加了累計來源
像素數（單位千像素避免溢位），之後就能算「每百萬像素幾毫秒」，跨批次直接比。

模型：`每張 ≈ 647 ms x 來源百萬像素 + 327 ms`。後面那個常數項是銳化＋抖動＋
旋轉，它做的是**目的地**（填滿時固定 480x800），不隨來源尺寸變 —— 把它也除以
來源像素數會重複計算，我第一次就是這樣把門檻算成 2.0 Mpx（實際是 2.6）。

### 15.9 讀執行中的系統，單張數值可能是撕裂的

用 SWD 讀正在播放的板子，`g_dbg_w` 可能來自第 N 張、`g_dbg_us_out` 來自第 N-1 張。
**只有累計值可信**（每張結束時才更新），單張的瞬時值不要拿來推理。

### 15.10 一個沒有效果的嘗試：PSRAM 改 write-back

原本整片 32MB 是 write-through 且 `NOT_BUFFERABLE`，等於每個 byte store 都是
獨立、同步、不能合併成 burst 的 PSRAM 交易。看起來像是重大瓶頸。

把純 CPU 暫存那半（0x91000000 以上 16MB：全尺寸 RGB、縮圖、播放清單、
資料夾索引、圖示備份）切成第 8 號區域改成 write-back。**端到端只賺約 3%。**

原因是瓶頸不在 store 的粒度，而在總流量。而 write-back 的 write-allocate
還多帶了一輪填線讀取（每張多 6.5MB），把省下來的抵銷掉大半。

保留這個設定（沒有壞處），但**別指望它**。真正的流量大頭是那個全尺寸 RGB888
中間層：被寫進 PSRAM 6.5MB、又被完整讀出來 6.5MB，而縮放對每個來源像素只用
一次。要省就得砍掉它（帶狀串流），不是調快取政策。

註：重疊時**編號大的區域優先**，所以覆蓋用的區域編號要大於底下那片。

### 15.11 換更好的濾波核是死路 —— 但要有對的尺才看得出來

問題：既然盒狀濾波在頻域上壓不乾淨超過 Nyquist 的成分，換 Mitchell 或
Lanczos 是不是能再降鋸齒？

**答案是不能，而且會更差。** 對「理論正確答案」的平均絕對差：

| 做法 | rings | star |
|---|---|---|
| 線性光盒狀（不銳化）| **14.33** | **4.79** |
| PIL 三次（Catmull-Rom）| 16.18 | 5.51 |
| 盒狀 + 銳化 100（本專案）| 16.62 | 5.62 |
| Mitchell + 銳化 100 | 17.45 | 6.14 |
| Lanczos-3 | 18.13 | 7.38 |

**越銳利的核離正確答案越遠。** 2.25 倍縮小的正確操作本來就是面積平均 ——
那正是感光元件在做的事。負瓣是為了「看起來銳利」刻意加入的失真，不是更正確。
所以縮小用盒狀／面積平均已經是對的，沒有升級空間。

### 15.12 用錯的基準會把對的方向判成錯的

這個結論一開始被我判反了，原因是基準選錯。

先前拿「線性光 Lanczos」當基準算平均絕對差。那把尺**獎勵銳利度**：
Mitchell 刻意用銳利度換無 ringing，於是分數更差（10.00 vs 盒狀 7.59），
我因此一度認定「Mitchell 實作有 bug」，還去追相位偏移、印權重表除錯 ——
權重其實完全正確。

**拿某個特定演算法當基準，等於預設它是對的。** 正確做法是回到問題本身：
測試圖樣是解析式定義的，直接在每個目的像素涵蓋的來源區域內超取樣
（6x6，線性光空間）求平均，那就是該像素應有的值，與任何演算法無關。
換上這把尺，排名整個反轉。

`test/groundtruth.py` 是這把尺。**任何「A 比 B 好」的畫質結論，先確認基準
不是 A 或 B 的近親。**

### 15.13 已經沒有剩餘空間了

換上正確基準之後回頭看本專案：盒狀+銳化 100 是 16.62、銳化關掉是 16.26、
PIL 的線性光盒狀是 14.33。銳化只佔 0.36；剩下約 1.9 的差距裡包含我們
刻意加的有序抖動（抖動用空間雜訊換掉色帶，逐像素比對一定會被扣分）
與邊界處理細節，有號平均只有 0.80，代表兩者沒有系統性偏差。

也就是說**重取樣這一段已經貼著理論極限了**。還想再降鋸齒只剩兩條路：
把銳化調低（很小，而且畫面會變軟），或者根本不要在板子上縮 ——
在 PC 上預先壓成顯示尺寸。實測比例 1:1 時本專案的重取樣器輸出與
「來源直接量化成 RGB565」**逐位元組相同**（100% 像素、最大誤差 0），
也就是完全不引入任何重取樣誤差。

### 15.14 研究級的「保細節」縮小演算法解的是相反的問題

查過三篇代表作：Kopf 2013（content-adaptive，讓取樣核隨影像特徵變形，EM 迭代）、
Öztireli & Gross 2015（以 SSIM 為目標函數，有封閉解、成本與線性濾波同級）、
Weber 2016（DPID，反向雙邊濾波，給偏離鄰域越多的像素越大權重）。

Öztireli 那篇是唯一在 MCU 上跑得動的，所以實作出來（`test/ssim_downscale.py`）
拿真實照片測。它的封閉解本質是**自適應局部對比補強**：縮小會壓低局部變異數，
它用 sqrt(輸入變異數/輸出變異數) 當增益把對比拉回去。

**結果是讓鋸齒更明顯。** 老鷹翅膀對天空那種高對比平滑邊緣，補強對比等於把
階梯的落差也放大。這類演算法的動機是「縮小把細節平均掉、照片變糊變平」，
跟「邊緣太硬有階梯感」是相反的訴求。

**專業軟體也沒有魔法**：Photoshop / Lightroom / ImageMagick 提供的就是
bicubic / Mitchell / Lanczos 這一家族加可選銳化，跟 15.11 量過的是同一批。

另外，「接近無損的縮小」在原理上不存在：1024 壓到 480 丟掉 78% 的取樣點，
資訊是可證明地消失的。那些論文做的不是「不損失」，是「選擇損失哪些」。

### 15.15 真實照片上唯一看得出差別的變數是銳化

合成圖樣（硬邊黑白）會誇大縮放演算法的差異、低估銳化的影響。改用真實照片
（`test/photos/`，從 picsum.photos 抓的 1200x1800）之後，把同一個高對比平滑
邊緣放大八倍並排比較，結論很乾脆：

- 換濾波核（盒狀 / Mitchell / SSIM 最佳化）：肉眼幾乎分不出來
- 銳化 100 / 60 / 30 / 0：邊緣的過衝亮暈與階梯感逐級消失，差異明顯

也就是說**要減少鋸齒不需要更強的演算法，需要少做一件事**。銳化的存在意義是
補償縮小造成的變軟，但它同時把邊緣過渡壓得更陡 —— 在 2.25 倍縮小後只剩
一兩個像素寬的邊緣過渡上，這個代價特別明顯。

判讀畫質時：**內容選錯會把結論帶偏**。合成圖樣適合量「誰比較接近正確答案」，
真實照片才判得出「使用者實際會看到什麼」。兩種都要。

---

## 十六、JPEG 硬體解碼：輪詢版量到的不是硬體速度

### 16.1 輪詢版是「CPU 逐字搬 FIFO」，比 DMA 慢 4.8 倍

`HAL_JPEG_Decode()`（輪詢版）的核心是
`while (JPEG_Process(hjpeg) == JPEG_PROCESS_ONGOING)`，而 `JPEG_Process` 就是
**CPU 手動搬運 JPEG 週邊的 FIFO**。同一張 2.16MP 的照片、同一塊板子：

| 路徑 | 每張 | 每 Mpx |
|---|---|---|
| `HAL_JPEG_Decode`（輪詢）| 305.6 ms | 141.5 ms |
| `HAL_JPEG_Decode_DMA` | **63.9 ms** | **29.6 ms** |

**相簿最初量到的 141 ms/Mpx 一直被當成「硬體解碼的成本」，其實那是 CPU
搬資料的成本。** 真正的硬體能力是 29.6 ms/Mpx —— 800x480 只要 11.4 ms，
足以支撐 20fps 以上的 MJPEG 播放。

一次只解一張的場景（相簿）用輪詢版是合理的取捨（少掉回呼與中斷設定），
但**不要把它量到的數字當成硬體極限**。

### 16.2 HPDMA 的區塊大小是 16 位元，而 HAL 直接遮掉不檢查

這是整件事裡最惡劣的陷阱。`DMA_CBR1_BNDT` 只有 16 位元（上限 65535 bytes），
而 HAL 的寫法是：

```c
MODIFY_REG(hdma->Instance->CBR1, DMA_CBR1_BNDT, (SrcDataSize & DMA_CBR1_BNDT));
```

**`& 0xFFFF` 遮掉，沒有任何檢查、沒有回傳錯誤。** 後果分兩種，第二種更糟：

| 傳入長度 | 遮完 | 症狀 |
|---|---|---|
| 10MB (`0xA00000`) | **0** | 硬體回報 USE（使用者設定錯誤），還算好查 |
| 618KB (`0x96FB2`) | 28594 | **不報錯，默默傳錯長度** |

第二種沒有任何錯誤訊息，資料就是不對，而症狀會出現在很遠的地方
（解出來的影像壞掉），完全不會聯想到 DMA 長度被截斷。

所以 **DMA 模式必須分塊餵**（本專案用 32KB），輸入靠 `GetDataCallback`、
輸出靠 `DataReadyCallback` 接續。ST 的 `JPEG_DecodingFromXSPI_DMA` 範例
用小緩衝區配回呼不是風格選擇，**是這個硬體限制逼出來的**。

註：這也修正了第 11.3 條的適用範圍。那條說「`DataReadyCallback` 裡不要呼叫
`HAL_JPEG_ConfigOutputBuffer`」，針對的是**輪詢模式下一開始就把整塊交出去**
的用法；**分塊 DMA 模式下在回呼裡接續餵緩衝區是本來就該做的事**。
用一個 `g_dma_mode` 旗標區分兩種行為。

### 16.3 別把實驗放進「每次都會執行」的路徑

第一版我把 DMA 通道設定與中斷啟用寫進 `HAL_JPEG_MspInit()` —— 那是照片路徑
每次都會走的初始化。結果輪詢解碼被干擾：**93 次失敗、1 次成功，相簿完全
不能用，而且重燒也救不回來**（壞的是每次都會執行的程式碼）。

正確做法：實驗的所有副作用關在一個函式裡，而且**由外部旗標觸發**
（用 SWD 寫 `g_dbg_bench_go`）。不觸發就完全等於不存在；出事重置一次就恢復。
測完做完整拆除：關中斷 -> `HAL_JPEG_Abort` -> `HAL_DMA_DeInit` 兩個通道 ->
解開 `hdmain`/`hdmaout` 連結 -> `HAL_JPEG_DeInit` + 重新 `Init` + 重建色彩表。
少做任何一步，之後的輪詢解碼都可能被影響。

還有一個相關的：**啟動了非同步硬體的失敗路徑一定要收乾淨**。DMA 啟動後
逾時就直接返回、沒有 `HAL_JPEG_Abort()`，週邊會停在忙碌狀態，之後每一次
解碼都拿到 `HAL_BUSY` —— 症狀是「第一張之後整個卡住」，看起來像解碼壞掉，
完全不會聯想到是先前那次失敗的實驗留下來的。

### 16.4 診斷靠硬體自己的錯誤碼，不要靠猜

DMA 接不起來時，把三個錯誤碼一起撈出來，一次就定位：

```c
g_dbg_bench_jerr     = HAL_JPEG_GetError(&g_hjpeg);   /* 4 = ERROR_DMA */
g_dbg_bench_derr_in  = HAL_DMA_GetError(&g_bdma_in);  /* 0 = 輸入正常 */
g_dbg_bench_derr_out = HAL_DMA_GetError(&g_bdma_out); /* 4 = ERROR_USE */
```

「輸入 0、輸出 USE」直接指向輸出通道的設定，而輸出長度正好是那個被遮成 0
的 10MB。**沒有這三個數字，我會在通道設定、埠分配、快取一致性之間繞很久。**

### 16.5 順帶：漏開 DMA 控制器時脈不會報錯

`__HAL_RCC_HPDMA1_CLK_ENABLE()` 在 ST 範例裡放在獨立的 `MX_HPDMA1_Init()`，
很容易漏。漏了的話 `HAL_DMA_Init()` 寫暫存器等於寫進虛空，**而且照樣回傳
`HAL_OK`** —— 症狀是設定看起來成功、傳輸永遠不開始。

### 16.6 DMA 在隔離模式完全正確，整合進正式路徑卻會卡死（未解）

隔離模式下驗證得非常乾淨：

| 檢查 | 結果 |
|---|---|
| 輸出長度 | 輪詢 6,480,000 = DMA 6,480,000 |
| 逐位元組比對 | **完全相同** |
| 校驗和 | 兩邊都是 `0x3D190426` |
| 連續 40 次 DMA 解碼 | **40 次全過、0 失敗** |
| 速度 | 30.7 ms/Mpx（輪詢是 141.5） |

**但整合進正式路徑就壞**：第一張成功，之後 `HAL_JPEG_Decode_DMA` 永遠回傳
BUSY，而且 `HAL_JPEG_Abort()` 救不回來 —— 連退回輪詢也一起失敗
（實測 `dma_ok=1`、`dma_fallback=1042`、`decode_ok=3` 對 `decode_fail=517`）。

兩者唯一的差別是**兩次解碼之間夾了什麼**：隔離測試是背對背連續呼叫，
正式路徑中間隔著 SD 讀檔、轉色、縮放、顯示與兩秒等待。根因還沒找到。

已經排除的：分塊邏輯本身（逐位元組驗證過）、連續次數（40 次沒問題）、
`ConfigOutputBuffer` 的無條件呼叫（ST 範例也是這樣寫）。

**教訓：「隔離測試全過」不等於「可以整合」。** 這個案例裡隔離測試涵蓋了
正確性、穩定性、效能三項，全部通過，整合後仍然壞 —— 因為它沒有涵蓋
「解碼之間發生的事」。設計隔離測試時要問的是「正式路徑有什麼是這裡沒有的」。

自動退回的設計值得保留：正式路徑先試 DMA、失敗就 Abort 並改用輪詢，
並記錄 `dma_ok` / `dma_fallback` / `dma_lasterr`。**這讓「實驗失敗」變成
「效能退回」而不是「功能損壞」**，而且失敗的頻率與時機直接顯示在計數器上。
（可惜這次連輪詢都被拖垮，所以最後仍然要整個退掉。）

### 16.7 順帶：自動開播讓除錯循環快很多

這塊板子沒有 UART，每次驗證都要「燒錄 -> 手動勾資料夾 -> 按開始 -> 等播放」。
一個 session 下來重複十幾次，而且中間的手動步驟沒有任何除錯價值。

`AUTO_PLAY`（`album_main.c`）讓掃描完直接開播，資料夾本來就預設全選 ——
插電就會動。相框本來就該是這個行為，順便讓遠端除錯不再需要人在旁邊。
往下滑回選單之後就走正常的手動流程，設定隨時改得到。

### 16.8 找到了：JPEG 週邊會累積狀態，Abort 清不掉，要 DeInit+Init

16.6 那個「連續 N 次 DMA 解碼後永久 BUSY」的未解問題，在影片專案裡解開了。

**症狀有高度一致性**：相簿是第 8 張、影片是第 24 格，之後
`HAL_JPEG_Decode_DMA` 永遠回傳 BUSY。次數不同但模式相同 —— 成功若干次後
突然永久卡死，而且**`HAL_JPEG_Abort()` 救不回來**（兩邊都試過）。

**解法是每次解碼後做完整的 DeInit + Init**：

```c
(void)HAL_JPEG_DeInit(&g_hjpeg);
g_hjpeg.Instance = JPEG;
(void)HAL_JPEG_Init(&g_hjpeg);
```

加上之後影片從「卡在第 24 格」變成連續播放數百格不停。成本每格幾十微秒，
相對於整格 70ms 可以忽略。

**所以是狀態累積而不是單次操作沒收乾淨**。真正累積的是什麼還沒查明
（懷疑跟「解碼結束時 HAL 會再呼叫一次 DataReadyCallback、而我們在那裡又餵了
一塊輸出緩衝區」有關），但 DeInit+Init 是可靠的繞法。

**同時修正 16.6 裡一個錯誤的推論。** 當時的隔離測試「連續 40 次全過」被我
讀成「穩定」，但那個測試是**跑固定 40 次就停**，根本沒證明第 41 次會成功。
影片這邊在第 24 格就失敗，回頭看那個 40 次很可能只是還沒撞到。
**固定次數的壓力測試只能證明「至少撐得住 N 次」，不能證明穩定** ——
要證明穩定得跑到失敗為止，或跑到遠超過實際用量的次數。

### 16.9 MJPEG 播放的實際瓶頸是轉色，不是解碼

800x480 的 4:2:0 影格，實測 499 格的平均：

| 階段 | 每格 | 佔比 |
|---|---|---|
| JPEG 解碼（DMA） | 2.5 ms | 3.6% |
| **YCbCr -> RGB 轉色（CPU，jpeg_utils）** | **64.0 ms** | **91.4%** |
| 換頁（等垂直消隱） | 1.9 ms | 2.7% |
| 合計 | 70.0 ms | **14.3 fps** |

**解碼只佔 3.6%**，而且比先前從相簿照片推算的 11.4 ms 還快 4.5 倍 ——
因為影片影格是 4:2:0（每像素 1.5 bytes），相簿那批照片是 4:4:4（3 bytes），
輸出資料量只有一半。**跨取樣格式外推是不可靠的。**

轉色佔 91%，而 DMA2D 支援 YCbCr 輸入（16.2 查證過）。這是下一步唯一該做的
最佳化 —— 使用者最初提的「用 DMA2D 做 YCbCr→RGB」在照片上只值兩成
（board-notes 15 那輪），在影片上值九成。**同一個想法，換個場景就從邊際
效益變成決定性槓桿。**

### 16.10 HAL_DMA2D_Start 只能用一次 —— 除非你也用 PollForTransfer

這個 bug 的欺騙性極高，值得完整記下來。

**症狀**：畫面在「第一格畫面」與「全黑」之間快速交替閃爍。

**已排除的（都量過）**：雙緩衝索引沒寫反（`g_front` 確實在翻，兩塊的寫入
次數 564 對 568 完全平衡）、LTDC 沒有 FIFO underrun（計數 0）、
DMA2D 設定完全正確（輸入 YCbCr、CSS=4:2:0、輸出 RGB565、800x480、無錯誤旗標）。

**根因**：`HAL_DMA2D_Start()` 會把 `hdma2d.State` 設成 BUSY，而負責設回 READY 的
是 `HAL_DMA2D_PollForTransfer()`。但 3.3 那條說過那個函式不可靠（等待迴圈包在
`if (CR & START)` 裡，快速傳輸會被整個跳過），所以我們改成自己輪詢 START 位 ——
**代價是 State 永遠停在 BUSY，第二次之後 HAL_DMA2D_Start 直接回傳 HAL_BUSY
什麼都不做**。而回傳值被 `(void)` 丟掉，完全沒有錯誤跡象。

所以程式邏輯正確地輪流指定兩塊 framebuffer，硬體的 `OMAR` 卻永遠停在第一次
那個位址，另一塊從頭到尾沒被寫過。

**解法**：每格直接寫暫存器啟動，完全不碰 HAL 的狀態機。Init/ConfigLayer
仍可用 HAL（一次性、不涉及狀態機）。

```c
DMA2D->FGMAR = src;  DMA2D->OMAR = dst;
DMA2D->NLR   = (w << DMA2D_NLR_PL_Pos) | h;
DMA2D->IFCR  = DMA2D_IFCR_CTCIF | DMA2D_IFCR_CTEIF | DMA2D_IFCR_CCEIF;
DMA2D->CR   |= DMA2D_CR_START;
while ((DMA2D->CR & DMA2D_CR_START) != 0u) { }
__DSB();
```

這其實是 3.3 早就給過的結論（相簿的 dma2d.c 一直是直接寫暫存器），
但我在新專案裡用了 HAL 的便利介面，就重新踩了一次。
**「同一個坑在不同專案裡會換一種樣子出現」** —— 3.3 記的是「等待不可靠」，
這次現形的是「狀態機卡死」，兩者是同一個根因的不同面向。

**找到它的關鍵是量測而不是讀碼**：程式邏輯看起來完全正確，只有把硬體的
`OMAR` 讀出來（永遠是同一個位址）跟軟體的計數（兩塊平衡）擺在一起，
矛盾才浮現。

### 16.11 更正 16.9：DMA2D 轉色不是「四千倍」

16.9 曾記錄「轉色從 64ms 變成 0.016ms」。**那是假的** —— 當時 DMA2D 因為
上述的 HAL_BUSY 根本沒執行，量到的是「什麼都不做」的時間。

修好之後的真實數字（800x480 4:2:0，1101 格平均）：

| 階段 | CPU 轉色 | DMA2D |
|---|---|---|
| 解碼 | 2.5 ms | 2.7 ms |
| 轉色 | 64.0 ms | **20.9 ms** |
| 換頁＋等待 | 1.9 ms | 18.8 ms |
| 合計 | 70.0 ms | **42.4 ms** |
| 能力 | 14.3 fps | **23.6 fps** |

DMA2D 讓轉色快 3 倍（不是 4000 倍），整體從 14.3 到 23.6 fps。仍然達成
20 fps 的目標，但餘裕遠比原本以為的小。

轉色 20.9ms 換算是 64 MB/s（讀 576KB + 寫 768KB），而 LTDC 同時持續佔用
46 MB/s —— **現在的瓶頸是 100MHz PSRAM 的頻寬，不是運算**。想再快就得
減少 PSRAM 往返（例如 YCbCr 中間緩衝區放內部 SRAM）。

**教訓：效能數字好得不合理時，先確認那段程式真的執行了。** 四千倍的加速
應該立刻引起懷疑 —— 我當時直接寫進了 commit 訊息與 board-notes。

### 16.12 分塊餵資料時，每一塊的長度都必須是 4 的倍數

影片有 26% 的影格解碼失敗（`jerr = HAL_JPEG_ERROR_DMA`）。根因是**餵給輸入
DMA 的最後一塊只有 1~3 個位元組**，而 HPDMA 的來源／目的資料寬度設成 word，
`BNDT` 不是 4 的倍數硬體就報 USE（user setting error）。

**HAL 在兩個地方對長度的處理方向相反**，這是陷阱的核心：

| 位置 | 做法 |
|---|---|
| `JPEG_DMA_StartProcess`（第一塊）| **捨去**：`len - len % 4` |
| `JPEG_DMAInCpltCallback`（續傳）| **進位**：`((len + 3) / 4) * 4`，但**只在 `len >= 4` 時**；`< 4` 是「Nothing to do」 |

於是：整包一次餵完的小影格，第一塊被捨去成 4 的倍數，`size % 4` 個位元組被
留到下一次；那個尾巴只有 1~3 個位元組，HAL 不幫忙進位，原封不動送進 DMA。

失敗集合完全可預測，實測 120 格：

| 影格類型 | 數量 | 失敗 |
|---|---|---|
| 單塊（`size <= CHUNK`）且 `size % 4 != 0` | 32 | **32（每一次都失敗）** |
| 單塊且 `size % 4 == 0` | 16 | 0 |
| 分塊（`size > CHUNK`，尾段夠長會被進位） | 72 | 0 |

32/120 = 26.7%，跟量到的失敗率完全吻合。

**解法**：自己保證每一塊都是 4 的倍數，不要相信 HAL 會處理。
影格包本來就把每格補齊到 4 位元組邊界，所以往上取整讀到的是補的 0，
而且落在 EOI 之後，解碼器不會理它。

```c
n = (n + 3u) & ~3u;                       /* 續傳的每一塊 */
first = (size > CHUNK) ? CHUNK : ((size + 3u) & ~3u);   /* 第一塊 */
```

**順帶修掉另一個違反協定的地方**：輸入用完時我們是直接 `return`，
但 HAL 的說明寫得很清楚 —— 要用 `HAL_JPEG_ConfigInputBuffer(hjpeg, ptr, 0)`
明講「沒有了」。直接 return 的話 `pJpegInBuffPtr` 與 `InDataLength` 都留著
上一次的值，而 `JPEG_DMAInCpltCallback` 只檢查 `InDataLength > 0` 就重啟
DMA，等於**把同一塊資料再送一次**。

修完：1940 格連續播放，**失敗 0**，`late` 從 406 變成 0，輸出長度每格都是
576,000 bytes（800x480 的 4:2:0，完整不截斷）。

#### 定位方法：先證明它是確定性的，再問「哪幾個」

這個 bug 值得記的不只是結論，還有找法。一開始手上只有「26% 失敗」，
看起來像競爭條件（每格中間夾著轉色、換頁、等待，很容易往那邊想）。

**第一步是算出它不是隨機的**：`fail / (fail + ok)` 除以圈數，得到 32.0 和 88.0
兩個整數 —— 120 格裡固定 32 格每圈都失敗。兩次取樣都給同一個數字。
確定性的 bug 和競爭條件要用完全不同的方法追，這一步省掉了整條錯路。

**第二步是讓硬體自己說是哪 32 格**：加一個位元圖（120 格 = 4 個字），
每格成功/失敗各設一個位元，一次 SWD 讀取就拿到完整的失敗集合。
`failmap & okmap == 0`（完全互斥）本身就再次證明了確定性。

**第三步是凍結第一次失敗的現場**：`CSAR / CBR1 / CTR1 / CSR` 加上 HAL 的
`InDataLength`、`pJpegInBuffPtr`。讀出來 `CBR1 = 2`、`CTR1` 是 word 寬度，
USE 的原因當場就寫在暫存器上，不必猜。**只留第一次**——後續的失敗會覆蓋掉，
而且錯誤碼會被之後的 DeInit/Init 清成 0。

拿到失敗集合之後，跟影格表對一次 `size % 4` 就收工了。
**「26% 失敗」和「這 32 格因為 size 不是 4 的倍數所以必定失敗」是同一件事，
但後者可以直接寫成修正。**

### 16.13 分帶轉色：YCbCr 中間層放內部 SRAM

16.11 的結論是「瓶頸是 100MHz PSRAM 的頻寬，不是運算」。每格的 PSRAM 流量：

| 動作 | 量 |
|---|---|
| JPEG DMA 寫 YCbCr | 576 KB |
| DMA2D 讀 YCbCr | 576 KB |
| DMA2D 寫 RGB565 | 768 KB（省不掉，LTDC 要從 PSRAM 掃描） |
| **合計** | **1,920 KB/格** = 24fps 下 46 MB/s，而 LTDC 同時也要 46 MB/s |

把 YCbCr 搬進內部 SRAM 可以砍掉前兩項。**但整塊放不下**：800x480 的 4:2:0
要 562KB，而內部 AXI SRAM 只有 **456KB**（連結腳本的 `__RAM_SIZE = 0x72000`，
不是憑印象猜的）。

**解法是分帶**：SRAM 裡只放幾條 MCU 列，解出一帶就立刻 DMA2D 轉進
framebuffer，576KB 從頭到尾不落 PSRAM。一條 MCU 列 = `寬/16` 個 MCU x
384 bytes（4:2:0），800 寬時是 19,200 bytes；480 高 = 30 條，取 2 條一帶
剛好整除成 15 帶。

#### 關鍵在 HAL_JPEG_Pause

輸出緩衝區只有一塊，填滿之後**一定要暫停**，否則 `JPEG_DMAOutCpltCallback`
會拿同一塊重啟輸出 DMA，在我們還沒轉完的時候就把它蓋掉（它只檢查
`JPEG_CONTEXT_PAUSE_OUTPUT` 旗標）。

```c
/* DataReadyCallback（中斷內）：只記長度並暫停 */
g_band_len = OutDataLength;
HAL_JPEG_Pause(hjpeg, JPEG_PAUSE_RESUME_OUTPUT);

/* 主迴圈：轉掉、餵回同一塊、恢復 */
band_drain();
HAL_JPEG_ConfigOutputBuffer(&hjpeg, g_band, BAND_BYTES);
HAL_JPEG_Resume(&hjpeg, JPEG_PAUSE_RESUME_OUTPUT);
```

**轉色不要放在回呼裡做。** 轉一帶要幾百微秒，在中斷裡忙等會把 SysTick
押後、`HAL_GetTick()` 漏拍，對時與逾時判斷全部失準。

#### 只在格式對得上時才分帶

`MCU_ROW_BYTES` 是照 4:2:0、寬 800 算死的常數，格式一變數字就錯，
而錯的行數會讓 DMA2D 寫超出 framebuffer。第一格一定走原本的整塊路徑
（那時 DMA2D 還沒設定），順便把取樣格式與寬高量出來，相符才開始分帶。

#### 實測（同一份韌體 A/B、各 2184 格）

| | 分帶關閉 | 分帶開啟 |
|---|---|---|
| 讀檔 | 2.84 ms | 2.94 ms |
| 解碼 | 3.66 ms | **2.06 ms** |
| 轉色 | 19.91 ms | **13.72 ms** |
| 換頁 | 7.98 ms | 8.40 ms |
| 合計 | 34.39 ms | **27.12 ms** |
| 能力上限 | 29.1 fps | **36.9 fps** |
| 跟不上素材格率 | 156 次 | **0 次** |

轉色快 31%，而且**解碼也快了 44%** —— 事前沒預期到這一點：JPEG 的輸出 DMA
現在寫的是 SRAM 不是 PSRAM，而它跟 DMA2D 搶的本來就是同一條匯流排。
**優化一個階段順便讓另一個階段變快，是「共用資源受限」的典型徵兆。**

A/B 用 `g_dbg_useband` 這個 SWD 可寫的旗標做，同一份韌體、同樣的影格 ——
重編一版再比會混進別的差異。

#### 更正：4 條 MCU 列那個結論是錯的

上面那張表裡「每帶 4 列 -> 638 格 0 失敗」**不能當成可行**。

4 條 MCU 列 = 76,800 bytes，而 **HPDMA 的 BNDT 只有 16 位元（上限 65,535）**。
HAL 是 `size & 0xFFFF` 遮掉不檢查（16.2），於是實際只傳了 11,264 bytes ——
更糟的是 `JPEG_GET_DMA_REMAIN_DATA` 用「原本的 OutDataLength 減 BNDT 餘量」
回報長度，所以**回報 76,800、實際只寫了 11,264**。解碼不會報錯，
畫面卻是大半舊資料。

我當時量到「638 格 0 失敗」就下了結論 —— 那量的是**解碼有沒有報錯**，
不是**畫面對不對**，而且我沒有看過螢幕。這正是第七章那條
「純數學測試通過、畫面卻整個上下顛倒」的同一個錯誤。

**兩個條件合起來，800x480 的 4:2:0 沒有合法的分帶大小：**

| 條件 | 限制 |
|---|---|
| BNDT 16 位元 | 一帶 ≤ 65,535 bytes -> 最多 3 條 MCU 列 |
| 不能整除總列數 | 30 條的因數 1/2/3 全部出局 |

所以分帶在這個幾何**無解**，影片專案已改成預設關閉（程式碼保留，
換成別的幾何時可能成立 —— 例如 720p 是 45 條，2 條一帶 38,400 bytes
又不整除 45，就可以）。

先前影片專案用 2 條列跑了 2184 格沒事是**僥倖**：那是個競爭，
換一份影格檔（讀檔延遲不同）就現形，實測變成每格都在第 29 帶失敗。

#### 已解：分帶大小不能整除輸出總量

同一套分帶搬進相簿會在第一個分帶影格就掛掉。追出來的根因跟 SRAM、
跟 Pause/Resume、跟中斷裡忙等都無關 ——

**當輸出總量剛好是分帶大小的整數倍時，最後一塊輸出填滿的瞬間正好也是
解碼結束，HAL 的收尾流程就走不下去，解碼永遠不回報完成。**

掃描列數 480/16 = 30 條，改每帶的列數掃一遍（同一份韌體、SWD 切換）：

| 每帶列數 | 30 ÷ 列數 | 結果 |
|---|---|---|
| 1 | 30.0 整除 | 失敗，轉帶 **29**（差最後一帶）|
| 2 | 15.0 整除 | 失敗，轉帶 **14**（差最後一帶）|
| 3 | 10.0 整除 | 失敗，轉帶 **9**（差最後一帶）|
| 4 | 7.5 **不整除** | **638 格 0 失敗** |

**每一次都剛好差最後一帶** —— 這個規律性本身就是「不是競爭條件」的證據。
之後靠 2 秒逾時收場，而逾時的 `HAL_JPEG_Abort` 又把週邊弄髒
（`g_dma_out` 停在 SUSPEND），所以後面每一格都在 `HAL_JPEG_Decode_DMA`
起跑點失敗 —— 那個 `-11` 只是二次症狀，不是病因。

註：影片專案用每帶 2 列（也是整除）卻沒事，因為那邊是
Pause/Resume + 主迴圈轉色，結尾的通道狀態不一樣。所以這條限制**跟架構有關**，
不能無條件套用；但「不要讓分帶整除輸出」是不用花錢的保險。

#### 追這個 bug 的方法（比結論更值得複製）

三個假設全被實測推翻（Pause/Resume 的時序、HPDMA 寫內部 SRAM、
DMA 通道殘留狀態），每一個都是關掉那個變因再跑一次。真正打開局面的是
**兩個計數器**：

- **每個失敗出口各一個計數**。`lasterr` 只留最後一次，看到的永遠是
  `-11`（起跑點失敗），我因此以為每一格都在起跑點掛掉。加上直方圖才看到
  真相是「**2 次逾時**，之後 506 次起跑點失敗」—— 病因與症狀完全是兩件事。
- **轉了幾帶**。29 這個數字直接指出「差最後一帶」，而換成 2 列變 14、
  3 列變 9，規律立刻浮現。沒有這個計數，我只會看到「就是壞」。

**「最後一次的錯誤碼」是最容易誤導人的診斷資料。** 級聯失敗裡它一定是
最後那個二次症狀。要分辨病因與症狀，就得讓每個出口各自留下計數。

#### 相簿仍然不套用分帶（量出來反而更慢）

修好之後在相簿量了 A/B（同一份韌體）：

| | 解碼 | 轉色 | 合計 |
|---|---|---|---|
| 不分帶 | 3.84 ms | 20.62 ms | **24.46 ms** |
| 分帶（每帶 4 列）| 15.29 ms | 13.27 ms | 28.56 ms |

**分帶反而慢 17%。** 轉色確實省下來了（20.6 -> 13.3），但解碼從 3.8 變成
15.3 —— 相簿的分帶是在中斷回呼裡逐帶忙等 DMA2D，等於把解碼整個串行化，
省下的 PSRAM 流量賠了回去。影片專案是主迴圈轉色，架構不同才有賺。

所以相簿維持不分帶（實測 762 格 0 失敗），程式碼也不留 —— 把量出來沒有
好處的複雜度留在正式路徑上沒有意義。**知識留在這裡就夠了。**

#### 快取

分帶緩衝區在內部 SRAM，是可快取的區域，但 **CPU 從頭到尾不碰它**：
JPEG DMA 寫、DMA2D 讀，兩者都繞過 D-Cache。沒有髒的快取列，
所以不需要任何 clean/invalidate。
---

## 十七、SD 卡：從電腦把大檔送進板子

沒有讀卡機時，可以讓板子當中介：SWD 把資料塞進 PSRAM，韌體收一塊寫一塊到卡上。
這一章記的是這條路上實際踩到的東西。

### 17.1 SWD 的實際吞吐量

| 設定 | 4MB 耗時 | 換算 |
|---|---|---|
| `freq` 未指定（預設 8000 kHz） | 7.82 s | 536 KB/s |
| `freq=24000`（ST-LINK V3 上限） | 4.69 s | **895 KB/s** |

`freq=50000` 會被夾到 24000，沒有更快。122MB 約三分鐘，一次性作業可以接受。

目的地寫 PSRAM（`0x91000000`）可行，前提是韌體已經把 XSPI 設成記憶體映射。

### 17.2 `-w32` 的回寫驗證會跟「韌體吃掉指令」打架

主機用 `-w32` 寫一個指令字給韌體，韌體處理完把它清成 0 —— 而 CLI 寫完會**讀回來
驗證**，於是驗證必然對不起來、**CLI 回傳非零**。腳本有 `set -e` 就會在這裡無聲中止。

症狀非常難查：trace 停在 CLI 那一行，沒有任何錯誤訊息（輸出被 `>/dev/null` 丟掉）。

**韌體把指令字清掉正是「做完了」的信號**，所以這個「失敗」要容忍：

```bash
wr32() { conn -w32 "$1" "$2" >/dev/null || true; }
```

真正的成功與否要靠自己的握手（輪詢指令字歸零 + 檢查錯誤碼）判斷，不要靠 CLI 的回傳值。

順帶一提，CLI 印的十六進位是**大寫**，`[ "$v" != "0000000a" ]` 這種比對會永遠成立。
一律轉小寫再比。

### 17.3 指令與參數要合併成一個字

「先寫長度、再寫指令」中間隔著一次完整的 SWD 連線（約 1.5 秒），韌體很可能在
兩者之間就讀到了半套狀態。把它們塞進同一個字就沒有這個問題：

```c
g_sdw_cmd = (opcode << 28) | payload;   /* payload 28 位元，蓋得住 16MB */
```

### 17.4 `FATFS_LinkDriver` 第二次呼叫一定失敗

FatFs 只有一個磁碟槽。播放路徑開機時已經掛接過同一個驅動，之後要進燒錄模式
再掛接一次就會失敗。**這不是錯誤，是常態** —— 直接沿用 `"0:"` 就好。

第一版在這裡 `for(;;){}` 卡死，症狀是「燒錄模式永遠不就緒」。

### 17.5 沒有來源時**不能**停在空轉迴圈

第一次使用時卡上還沒有影片檔，Flash 也是空的。如果這時候韌體停在
`for(;;){}`，主機就沒辦法把它叫進燒錄模式 —— 變成「要有影片才裝得進影片」的
雞生蛋問題。找不到來源的閒置迴圈裡一定要繼續輪詢燒錄旗標。

### 17.6 偵錯器寫進去的資料，CPU 讀之前要失效快取

主機經 SWD 寫 PSRAM 是**完全繞過 CPU 的 D-Cache** 的。接著 FatFs 會用 CPU 去讀
那塊記憶體交給 SD，讀到的可能是上一輪殘留在快取裡的舊內容。

跟第 3.1 條那個 DMA2D 的坑同一類：**硬體（這裡是 SWD）寫、CPU 讀，一定要手動失效**。
位址與長度都要對齊到 32 位元組的快取列。

```c
SCB_InvalidateDCache_by_Addr((uint32_t *)SDW_BUF, (int32_t)((len + 31u) & ~31u));
```

### 17.7 已解：不要用 CREATE_ALWAYS 覆寫大檔，要原地覆寫

**症狀**：`f_open(SDW_PATH, FA_WRITE | FA_CREATE_ALWAYS)` 在檔案已經存在時
回傳 `FR_DISK_ERR`。建立新檔完全沒問題（實測連續寫入 4.58MB 一次過）。

**根因**：`CREATE_ALWAYS` 會先把舊檔的**整條簇鏈釋放掉**。釋放一個上百 MB
的檔案等於連續打出上百次**單磁區**的 FAT 寫入，實測約 130 次之後卡片就不再
回應指令。

現場（靠 `g_sd_*` 那組變數撈出來的）：

| 量到的 | 值 |
|---|---|
| `BSP_SD_WriteBlocks` 回傳 | -4（PERIPH_FAILURE） |
| `HAL_SD_GetError` | 4 = **CMD_RSP_TIMEOUT** |
| `hsd.State` | 1 = READY（軟體狀態機正常） |
| `BSP_SD_GetCardState` | BUSY |
| 失敗磁區 | 6124 / 6126（兩次獨立實驗，都在 FAT 區且相鄰） |

**解法：改成原地覆寫，一個簇都不要釋放。**

```c
f_open(&fil, path, FA_WRITE | FA_OPEN_ALWAYS);   /* 不截斷，保留簇鏈 */
f_lseek(&fil, 0);
/* ...從頭蓋過去... */
if (written < oldsize) { f_truncate(&fil); }     /* 只在新檔比較小時 */
f_close(&fil);
```

- 新檔比舊檔**大** -> 超出的部分照常配置新簇，走的是「建立新檔」那條好路
- 新檔比舊檔**小** -> 關檔前才截斷，而且只釋放多出來的尾巴

把 `f_truncate` 放在**資料全部寫完之後**還有一個好處：那是唯一會釋放簇鏈的
動作，就算它失敗，檔案內容也已經是完整的。

實測：122MB 覆寫 192 秒完成，長度分毫不差，之後從卡上播放 7199 格 0 失敗。

**教訓：「同一個 API 的兩種用法」可能走完全不同的程式路徑。**
我一開始把它讀成「這張卡的寫入不可靠」，還去懷疑時脈餘裕與卡片壽命 ——
但「建立新檔一次過、覆寫舊檔必掛」這個對比早就指出問題不在寫入本身，
而在**釋放簇鏈**這個特定動作。三個量到的數字（成功建立、失敗覆寫、
失敗磁區都在 FAT 區）擺在一起就足以定位，我卻先去查卡片品質。

註：釋放簇鏈為什麼會讓這張卡停止回應，仍然沒有查明 —— 有可能是這張卡在
密集單磁區寫入下的弱點。但正式路徑已經完全不走那條路了，所以不再追。

### 17.8 外部 NOR Flash 又多了一個固定壞格

原本記錄的是 `0x70170466`（bit 2 抹除後回不到 1）。這次燒錄影片韌體時，
讀回比對又抓到第二個：

| 位址 | 應為 | 實際 | 差異位元 | 落在 |
|---|---|---|---|---|
| `0x70170466` | — | — | bit 2 | 查表資料 |
| **`0x700217C6`** | 0xD6 | 0xD2 | **bit 2** | `oem2uni932`（FatFs 字碼頁表） |

連續讀三次都是 0xD2，是固定的不是讀取雜訊（board-notes 10.5 提醒過驗證
階段的 mismatch 可能是假警報，所以一定要另外讀回來確認）。

**兩個壞格壞的都是 bit 2**，值得記一筆 —— 這比較像是這顆 NOR 的某條
位元線在特定區塊上比較弱，而不是隨機的耗損。

目前不影響執行：`oem2uni932` 只在轉換非 ASCII 長檔名時才會查，
而影片專案的檔名都是 ASCII。但**如果哪天壞格落在 .text 的函式裡就不能忽略**，
要調整版面避開。`flash.sh` 每次燒錄都會讀回整份比對並指出落在哪個符號，
就是為了讓這個判斷有依據。

### 17.9 一個對比很有價值的觀察

同一張卡、同一套程式：
- **讀取**：數千次零錯誤
- **大塊循序寫入**：4.58MB 一次過
- **密集單磁區寫入**：約 130 次後死

三者的差異本身就是線索：問題不在「能不能寫」，而在**寫入的節奏與粒度**。
下次追這個先從「每次寫入之間強制等到 `SD_TRANSFER_OK` 並加最小間隔」開始，
而不是從卡片品質開始懷疑。

---

## 十八、觸控介面（相簿第四輪：把手勢換成看得見的介面）

這一章的坑幾乎都不是「硬體」的，但每一個都花了好幾輪才找到，而且**症狀
彼此看起來毫無關聯**：反應慢、按了沒用、只有實體鍵有效、按鈕按不到。

### 18.1 同一次觸碰被兩個迴圈各吃一次

**症狀**：按「繼續」沒用。實體鍵長按可以繼續，觸控不行。

**根因**：暫停迴圈判到「繼續」→ 恢復播放 → 播放迴圈的「點畫面暫停」把
**同一次觸碰**又算了一次 → 立刻彈回暫停。手指還在螢幕上，兩個迴圈都看得到它。

**實體鍵沒事，是因為它是閂鎖旗標**（ISR 設一次、被讀走就清掉），
不會被兩個地方各讀一次。使用者說「只有實體鍵按得動」的時候，
那句話本身就是答案 —— **兩種輸入的差別在哪裡，bug 就在哪裡。**

**解法**兩道：
1. 回報「繼續」之前先等手指離開（`wait_release()`）。
2. 恢復播放之後 700ms 內不理會觸控，連放開瞬間的彈跳也擋掉。

翻頁那幾條不需要，因為它們後面接著 1.5 秒的解碼，手指早就離開了。

**推廣**：一個事件如果會讓程式**換到另一個也在讀同一個輸入的狀態**，
就必須在切換前把該事件消費掉。這跟 14.5 那個「旗標的設定與清除必須對稱」
是同一個家族。

### 18.2 「最後一次的錯誤碼」是最會騙人的診斷資料

追上面那個 bug 時，`g_dbg_lasterr` 永遠是 `-11`（在起跑點失敗），
所以我以為每一格都掛在同一個地方，往「呼叫前狀態不乾淨」查了很久 ——
呼叫前的狀態全部乾淨，於是卡住。

加上**每個失敗出口各一個計數**（直方圖）之後，真相是
「**2 次逾時，之後 506 次起跑點失敗**」。逾時才是病因，起跑點失敗是它的
後果（逾時的 Abort 把週邊弄髒）。

**級聯失敗裡，最後一個錯誤碼一定是二次症狀。** 要分辨病因與症狀，
就得讓每個出口各自留下計數。這一招在 16.13 那個分帶 bug 也是決定性的。

### 18.3 畫面上寫著功能，程式裡沒有做

選單畫面上寫著「上下滑動看更多」，`g_scroll` 也宣告了、繪製與命中判定都
用了它 —— **但從來沒有任何一行寫入它**。所以第六個以後的資料夾看不到也
選不到，而畫面還在教使用者怎麼操作。

影片清單更糟：連夾制都沒有，第 10 部開始壓到「返回」鈕、第 11 部以後直接
畫到畫面外。

**教訓**：`grep` 一個變數時要分清楚「被讀」與「被寫」。只被讀不被寫的狀態
變數，等於一個寫死的常數 —— 而它旁邊通常有一段假裝功能存在的 UI 文字。

### 18.4 「反應慢」通常是好幾個延遲疊在一起

使用者說按鈕反應慢。拆開來量：

| 來源 | 延遲 |
|---|---|
| 輪詢間隔 `nap(30)` | 30 ms |
| `wait_release()` —— **動作要等手指離開才觸發** | 220 ms |
| 每次重進暫停都閃圖示 `nap(1000)` | 1000 ms |
| 點畫面進暫停時又 `wait_release()` 一次 | 220 ms |

合計約 **840ms** 才按得到第一下。單獨看每一項都「還好」，疊起來就是「很鈍」。

拿掉的順序也有講究：**先拿最大的那個**（暫停圖示 —— 控制列跳出來本身就是
回饋，圖示是多餘的），再處理其餘。防彈跳的 4 次確認保留，只把輪詢間隔縮短
—— 那是 14.7 的教訓，砍掉會變成「一按翻兩張」。

### 18.5 按下就動作 vs 等放開才動作

兩者都對，看後面接什麼：

| 後面接著 | 該怎麼做 |
|---|---|
| 一段耗時工作（解碼 1.5 秒）| **按下就動作**。手指在那段時間裡自然會離開，延遲藏得掉 |
| 立刻切換到另一個也在讀觸控的狀態 | **等放開才動作**，否則就是 18.1 |

### 18.6 疊加層畫在「顯示中」的那塊 buffer，存還原必須成對

暫停時的控制列直接畫在正在顯示的 framebuffer 上（重新解一張照片要 1.5 秒，
存還原只要幾毫秒）。所有進出口都收斂到 `ov_show()` / `ov_hide()` 一對函式，
不讓任何一條路徑自己去 backup/restore —— 漏掉一次，控制列就會永久烙在
那塊 buffer 上。

存還原的搬移方向要看畫布方向：直立時邏輯 x 是實體列，所以沿著邏輯 y 走
才是連續記憶體（跟 2.6 的座標轉換同一件事）。

### 18.7 疊加層要不要跟著照片轉

相簿的 AUTO 模式會**逐張**決定畫布方向。第一版控制列跟著 `gfx_width()` /
`gfx_height()` 走，於是按鈕也跟著翻 —— 但使用者的手一直在同一個地方，
**會轉的應該只有照片**。

而且 `read_touch()` 回傳的**永遠是直立座標**（跟畫布方向無關），
所以橫向時「用橫向座標判定、拿直立座標來比」根本按不準。
統一成直立座標之後，版面固定與判定正確一次解決。

### 18.8 遠端追觸控 bug 的兩個鉤子

觸控沒辦法用 SWD 重現，只能靠使用者回報 —— 一來一回很慢，而且描述常常
跟實際發生的事不一樣。兩個鉤子讓遠端也追得動（都是旗標觸發，不寫就等於
不存在）：

- **注入按鍵**：`g_dbg_inject` 寫入動作代碼，就當成按了那一顆。
  用它證實了「繼續」的流程完全正常（`decode_ok` 持續增加、暫停迴圈沒重進），
  所以問題在「有手指」跟「沒手指」的差別 —— 那一步直接把範圍縮到 18.1。
- **記憶體內的假資料**：`g_dbg_fakedirs` / `g_dbg_fakevids` 假裝卡上有很多
  資料夾／影片，用來測捲動。**不必為了測 UI 去改使用者的 SD 卡內容**，
  重開機就沒了。
- **韌體端的自動測試**：`g_dbg_favtest = N` 讓韌體自己挑 N 張照片收藏、
  再把最愛全部播一遍，結果放進一組 `g_dbg_ft_*`（見 20.8）。
  **這比用 SWD 一步一步戳好得多**：每次連線要一秒多，戳十幾步就好幾十秒，
  而且節奏跟使用者用手點完全不同 —— 有些失敗只在連續操作時才出現，
  慢慢戳反而重現不了。上線第一次跑就抓到一個手動完全測不到的 bug。

### 18.9 使用者改變主意時，先問「它們是不是同一件事」

拉桿被我砍掉又加回來。砍的理由是「翻頁改成整片畫面都能拉之後，拉桿只剩
裝飾」；但使用者說「拉桿是真的可以用的」—— 他是對的，兩者根本是不同的東西：

| | 用途 |
|---|---|
| 拉桿 | **粗調** —— 一下跳到四千張裡的大概位置 |
| 整片畫面左右拉 | **微調** —— 前一張／後一張 |

我把「功能重疊」誤判成「互相取代」。四千多張照片攤在 400px 的拉桿上，
一個像素等於十張 —— 這正是它只能粗調、也正是它不可能取代逐張翻頁的原因。
**同一個數字（一像素十張）同時解釋了它的用途與它的極限。**


---

## 十九、PSRAM 時脈對格率的影響（實測）

第十章把 PSRAM 從 200MHz 降到 100MHz 是**正確性**的必要措施（這塊板子在
200MHz 下整片 16MB 掃描零個乾淨區塊）。那時的結論是「LTDC 只需要 46 MB/s，
100MHz 的頻寬遠遠夠用，沒有效能代價」—— 對 LTDC 而言是對的，
**但對 DMA2D 的轉色不是**。

### 量法

200MHz 下 PSRAM 讀取會出錯，所以要把**資料路徑**都避開它，只留下
「寫 framebuffer」這條會被弄壞的：

- 影格來源改用外部 Flash（記憶體映射、就地解碼，完全不經 PSRAM）
- 關掉素材格率對時（開著的話「等垂直消隱」那一欄是被對時相位決定的，
  不能拿來推算上限）

畫面因此是雜訊，但**時間量測完全有效** —— DMA 照樣跑完，只是資料錯。

### 結果（各 900+ 格，0 失敗）

| | 100MHz | 200MHz |
|---|---|---|
| 解碼（JPEG DMA 寫 PSRAM） | 2.50 ms | 2.38 ms |
| **轉色（DMA2D 讀 PSRAM 寫 PSRAM）** | **21.03 ms** | **13.11 ms** |
| 等垂直消隱 | 11.19 ms | 1.86 ms |
| 合計 | 35.19 ms | 17.63 ms |
| **自由跑格率** | **30.4 fps** | **60.8 fps** |

**剛好兩倍，而且 60 就是面板的上限。**

### 為什麼是「剛好兩倍」而不是 38%

轉色只快了 38%（21.03 -> 13.11），但格率翻倍 —— 因為 `present()` 會等垂直
消隱，**格率被 60Hz 面板量化成 16.67ms 的整數倍**：

| | 每格工作量（不含等待） | 需要幾個更新週期 |
|---|---|---|
| 100MHz | 23.5 ms | 2 個 -> 33.3ms -> 30 fps |
| 200MHz | 15.5 ms | **1 個** -> 16.67ms -> 60 fps |

**跨過 16.67ms 這道門檻就直接翻倍。** 反過來說，任何讓每格工作量從 17ms
降到 16ms 的優化都值 30 -> 60 fps，而從 25ms 降到 18ms 卻一格都不會多 ——
**面板量化讓效能收益是階梯狀的，不是線性的。** 優化之前先算自己落在哪一階。

### 所以換得動 200MHz 的板子值不值得

值 —— 格率從 30 翻到 60，而且那是面板天花板，沒有更高的了。

但要注意這**不一定是換 PSRAM 晶片就能解決的**：第 10.2 條量到的錯誤是
**均勻散布在整片**（不是集中在特定區塊），而且移動取樣點與 DHQC 都沒有
明顯改善 —— 那是整個資料視窗在 200MHz 下不夠，屬於**訊號品質**問題，
跟 PCB 走線、供電、晶片批次都有關，不只是記憶體顆粒本身。
換一塊板子可能就好，換一顆晶片焊上去未必。

---

## 二十、SD 卡寫入：把唯讀的相簿開一個小口

相簿一直是**唯讀**的（磁碟層回報 `STA_PROTECT`），為了「我的最愛」要複製
照片到卡上，才第一次在正式路徑開了寫入。這一章記的是那條路上的坑 ——
**每一條都是先把使用者的相框弄壞才知道的**。

### 20.1 阻塞式的 SD 操作一定要跟看門狗一起想

**症狀**：使用者回報「加入最愛失敗，然後連播放或返回都不行」。

**根因**：卡片拒絕寫入 -> 磁碟層重試 4 次、每次前後各等 `sd_wait_ready()`
2 秒 -> 單一磁區最壞 16 秒，而相簿的獨立看門狗正好是 16 秒，中間沒有人餵。
板子被 IWDG 打掉重開，開機讀到 `RCC_FLAG_IWDGRST` 就停在
「請拔出記憶卡再重新插入」並卡在 `while (sd_present())` —— 所以「什麼都不能按」。

影片專案那份寫入路徑是在**專用的燒錄模式**跑的，那裡的看門狗處境不同。
**把一段可能阻塞十幾秒的程式搬進正式路徑時，要重新檢查它跟看門狗的關係。**

解法：磁碟層開一個 keepalive 回呼，等待迴圈裡呼叫；重試次數也從 4 減成 2
（使用者在等的互動，早點回報失敗比多試兩次有用）。

### 20.2 輪詢 CMD13 不能全速空轉，寫入的逾時要比讀取長一個數量級

這兩個是同一次修好的，而且它們才是「這張卡寫不動」的真正原因。

`BSP_SD_GetCardState()` 每次都送一個 CMD13。原本的等待迴圈是
**沒有任何延遲的全速輪詢**，等於在卡片做內部寫入時瘋狂灌指令。
而且讀寫共用同一個 2 秒逾時 —— 便宜的卡在內部垃圾回收時單次寫入偶爾要
好幾秒，2 秒就放棄之後我們又送下一個指令給還在忙的卡片，就一路壞下去。

| | 修之前 | 修之後 |
|---|---|---|
| 連續加入最愛 | 第 2 張就死，連讀取一起癱瘓、要重置才回得來 | 連續 4 次操作 0 失敗 |
| `g_sd_werr` / `g_sd_wtimeout` | -4 / 3 | **0 / 0** |

```c
while (BSP_SD_GetCardState(SD_INSTANCE) != SD_TRANSFER_OK) {
    keepalive();
    if (HAL_GetTick() - t0 > limit_ms) { return STA_NOINIT; }
    HAL_Delay(1);              /* 不要全速灌 CMD13 */
}
```

讀取用 2 秒、寫入用 8 秒。**「卡片壞了」在下這個結論之前，先確認自己的
等待迴圈有沒有做這兩件標準的事。** 我差一點就判它死刑了。

### 20.3 f_mkdir 會做密集單磁區寫入，這張卡撐不住

FatFs 的 `f_mkdir` -> `dir_clear()` 在 `FF_USE_LFN != 3` 之下是**一次一個磁區**
把整個目錄簇清成 0，原始碼註解自己就寫著
"many single-sector writes may take a time"（`ff.c:1685`）。

這張卡的簇是 32KB = 64 個磁區，實測**每次都在第 33 次寫入**時卡片進 BUSY
就不再回應。兩次跑出完全相同的數字 —— 而 17.9 建議的「每次寫入之間加最小
間隔」也沒有任何改善。**確定性的失敗不是耗損也不是時序，是路徑本身。**

更糟的是失敗時簇已經配置出去，卡上會留下遺失簇，重試就一直累積。

解法：**韌體不建資料夾**，請使用者在電腦上建好。複製檔案本身沒有這個問題
（FatFs 對大塊資料是多磁區寫入，實測 302KB 只用 24 次 `disk_write`）。

註：同一個限制之後還會回來 —— 目錄裝滿一個簇要配置第二個簇時，又是 64 次
單磁區寫入。副本檔名長，一個 32KB 的簇大約放 200 個檔案。

### 20.4 解鎖寫入不需要重新掛載（這個假設害我弄丟檔案系統）

`STA_PROTECT` 是在 `disk_initialize` 決定的，所以第一版照影片專案那樣做成
「卸載 -> 解鎖 -> 掛載 -> 動手 -> 卸載 -> 上鎖 -> 掛回去」。

**那個前提是錯的。** `ff.c:3416-3419`：volume 已經掛載時，`mount_volume()`
仍然每次都呼叫 `disk_status()` 並重新檢查 `STA_PROTECT`。也就是說
**只要 `disk_status()` 重算保護位元，解鎖就立刻生效**，一次掛載都不用動。

而那一版的代價是真的踩到了：卡片拒絕寫入時，收尾那次 `f_mount` 也一起失敗，
檔案系統就停在**卸載狀態**，相簿之後每次 `f_open` 都失敗（`g_decode_fail`
一路飆到一萬多）。**多做一個不必要的步驟，就多一個會失敗的地方。**

### 20.5 為了省事加的快取，在它的理由消失之後會變成 bug

為了讓上面那個「重新掛載」便宜一點，我在 `disk_initialize` 加了一個
「卡片還在線就跳過 `BSP_SD_Init`」的捷徑。後來 20.4 確認重新掛載根本不需要，
捷徑就沒有理由存在了 —— 但我留著它。

然後它就咬人了：拔卡期間 `sdbsp_status()` 不保證會被呼叫到（相簿的
`sd_present()` 直接走 BSP、不經過 FatFs），旗標停在「還在線」，插回來時
跳過完整初始化，`f_mount` 直接回 `FR_DISK_ERR`，畫面變成「記憶卡無法讀取」。

**拿掉一個機制的時候，要把當初為它加的所有東西一起拿掉。**

### 20.6 這一輪的診斷順序

值得複製的不是結論而是順序 —— 每一步都把範圍砍掉一半：

1. **先分清楚是防寫還是壞掉。** `FR_WRITE_PROTECTED`(10) 與 `FR_DISK_ERR`(1)
   是完全不同的兩件事。記下 FatFs 自己的回傳碼，不要只記自己的錯誤列舉 ——
   我第一版只有「mkdir 失敗」，等於還在猜。
2. **看失敗的磁區在哪。** 落在 FAT 區（低位址）是 17.7 那類；落在資料區
   是別的問題。這一步直接排除了「釋放簇鏈」。
3. **問它是不是確定性的。** 兩次都停在第 33 次寫入 -> 不是耗損、不是競爭。
   這一步直接排除了 17.9 的「密集寫入把卡磨壞」，也讓我不必再試更長的間隔。
4. **去讀那個函式的原始碼。** `dir_clear()` 的註解直接寫著答案。
5. **最後才懷疑硬體。** 而且在懷疑之前，先確認自己的等待迴圈有沒有做對
   （20.2）—— 結果問題就在那裡，卡片是好的。

**我在第 3 步之後就一度宣告「這張卡不能寫」，那是錯的。**
真正的原因是 20.2 那兩個我自己沒做對的標準做法。

### 20.7 最後這個功能整個重做了 —— 而且是因為「用起來」才發現

上面 20.1~20.6 全部是為了「把照片複製到最愛資料夾」踩出來的。功能做完、
驗證通過（連續四次操作零失敗、長度逐位元組相符），**然後使用者實際用了一次**：

- 「加入最愛有跑完，但最愛資料夾沒有」→ 相簿只在開機／拔插卡片時掃描，
  複製完的照片要重開機才出現在選單裡。這件事測試時完全不會發現，
  因為我每次驗證都在重開機。
- 「第二次加入最愛就失敗了」→ 他那張照片 598KB，比我測試用的 140~377KB 大，
  寫入量更大，卡片又進 BUSY。

於是他問「有沒有更簡單的方法，最愛的照片做記號編號就好」—— 對的。
改成一個**固定大小、原地覆寫**的清單檔之後：

| | 複製照片 | 清單 |
|---|---|---|
| 每次收藏的寫入 | 一張 600KB 約 1200 個磁區 | **2 個磁區** |
| 選單更新 | 要重開機 | 馬上（清單在記憶體裡）|
| 數量上限 | 約 200（目錄簇）| 清單容量說了算 |
| 建立清單檔 | — | 64KB 一次寫完，**7 次 disk_write** |

關鍵是**檔案大小從頭到尾不變**：每筆固定 256 bytes，改一筆就是 `f_lseek`
到那一格覆寫 256 個位元組，FatFs 只做一個磁區的 read-modify-write ——
不配置新簇、不釋放簇鏈、不動 FAT。而釋放簇鏈正是 17.7 把這張卡搞死的動作。

**兩個一般性的教訓：**

1. **「驗證通過」跟「用起來對」是兩件事。** 我的驗證迴圈是
   「SWD 注入 -> 讀計數器 -> 重開機重新掃描」，而重開機那一步正好
   把「選單不會即時更新」這個問題洗掉了。**測試流程本身會掩蓋掉某些 bug，
   而使用者的流程不會。** 這跟第七章「純數學測試通過、畫面卻上下顛倒」
   是同一個家族 —— 測到的東西不等於使用者會遇到的東西。

2. **使用者提的簡化往往是對的，而且他不需要知道實作。** 他說的是
   「做記號就好」，翻成實作就是「用索引取代複製」—— 寫入量差 600 倍、
   同時解決了選單不更新與數量上限。我先前之所以選複製，是因為他要
   「電腦上也看得到」，但**那個需求用一份文字清單也滿足了八成**，
   而我沒有把「八成滿足但成本低 600 倍」這個選項擺到檯面上。

### 20.8 測試資料剛好避開 bug ── 「檔名裡有空白」

清單版上線之後使用者回報「加最愛功能不行，最後卡死」。自動測試一跑就看到：
**收藏 12 張全部成功、零錯誤，但最愛模式只找回 2 張。**

寫入完全正常，是**比對**錯了。紀錄是固定寬度、用空白補齊的，而我拿空白
當結束符 —— 使用者的資料夾叫 `Ayu Makihara (...) 77 set in 1`，
路徑一有空白就被截斷，比對永遠失敗。

**而我先前的三張測試照片全在 `ai_generated/` 底下，路徑裡一個空白都沒有，
所以測試全部通過。** 這已經是這個功能第三次踩到同一類問題：

| 輪次 | 測試看到的 | 使用者遇到的 | 差在哪 |
|---|---|---|---|
| 複製版 | 每次都成功 | 加了看不到 | 我每次驗證都重開機，正好洗掉「選單不會即時更新」|
| 複製版 | 140~377KB 沒問題 | 598KB 失敗 | 我挑到的照片比較小 |
| 清單版 | 收藏、播放都對 | 完全播不出來 | 我挑的資料夾名稱沒有空白 |

**測試資料的「乾淨」本身就是一種偏差。** 挑照片時要刻意挑最醜的：
名稱有空白、有中文、有括號、路徑最深、檔案最大的那幾張。

順帶一個實作細節：修正的做法是**從固定的結尾往回剝掉補的空白**，而不是
找終止字元。順便把「取出字串再 strcmp」改成直接跟紀錄 memcmp ——
絕大多數格子第一個位元組就不同，四千張照片乘上兩百多格也不費事。

### 20.9 讀取的失敗路徑也要餵狗

20.1 只在 `sd_wait_ready()` 裡加了 keepalive，但 `sdbsp_read()` 的失敗路徑
**根本走不到那裡** —— `BSP_SD_ReadBlocks` 一失敗就直接 return。

卡片進 BUSY 之後每一次讀取都是這條路，而上層會把整份播放清單重試一遍，
於是 16 秒的看門狗照樣把板子打掉。我加的「連續失敗就重新初始化」偵測
完全沒機會跑到，因為看門狗先動手。

**加了一個保護之後，要問「所有的失敗路徑都經過它嗎」。** 我只檢查了
「等待很久」這條，漏掉「立刻失敗但重複很多次」那條 —— 後者累積起來一樣久。

順帶把看門狗重置之後的處理也改了：原本是停在「請拔出記憶卡再重新插入」
等人來拔卡，現在先自己 `BSP_SD_DeInit()` 斷電再試一次。
**相框放在桌上自己卡死、要人來拔卡才會動，是最糟的失敗模式。**

### 20.10 把「是不是卡片的問題」量出來

追了好幾輪都繞在「收藏失敗」上，而每一輪都同時牽涉到最愛的邏輯與卡片，
分不開。最後做了一個**只有寫入、沒有任何最愛邏輯**的壓力測試
（`fav_stress()`，SWD 寫 `g_dbg_wrtest = N` 觸發）：對清單檔裡一個沒在用的
格子，反覆寫入同樣的 256 個位元組。

- 同一個磁區、同樣的內容、檔案早就存在
- 不配置簇、不釋放簇、不動 FAT、不建目錄
- 每次就是 open -> lseek -> write 256B -> 讀回比對 -> close

**結果：第 15 次成功，第 16 次失敗，之後永遠失敗。**

| | |
|---|---|
| 成功寫入 | **15**（之後凍在 15 不再增加）|
| 第一次失敗 | 第 **16** 次 |
| 同期讀取 | 3,734 次、**0 錯誤** |
| 失敗當下 | `BSP_SD_WriteBlocks` = -4、卡片 BUSY、`HAL_SD_GetError` = 0 |

**把變因減到只剩一個，答案就沒有爭議了。** 先前每一輪都是「收藏失敗」
加上「我又找到一個 bug」，兩者纏在一起，所以一直不敢下結論 —— 而那些 bug
確實都是真的（路徑比對、看門狗、掉資料），修掉也都有改善。
但**只要測試裡還留著自己的邏輯，就永遠證明不了問題不在自己這邊**。

這個測試值得留著：換卡之後跑一次 `g_dbg_wrtest = 100`。
好的卡應該是 100/0，這張是 15/85。

### 20.11 「檔案讀不到」不等於「檔案不存在」

這是整輪最嚴重的一個 —— **它會弄丟使用者的資料**。

`fav_init()` 原本是這樣：

```c
if (f_open(&f, path, FA_READ) == FR_OK) { ...載入... return; }
create_file();          /* 任何失敗都當成「檔案不存在」 */
```

`f_open` 失敗有兩種完全不同的意思：`FR_NO_FILE`（真的沒有，該建）與
`FR_DISK_ERR`（這一瞬間讀不到）。而這張卡偶爾就是會有那一瞬間 ——
於是韌體用 `FA_CREATE_NEW` 造了一個新的空清單，**使用者 41 筆收藏變成 1 筆**。

```c
r = f_open(&f, path, FA_READ);
if (r == FR_OK)                              { ...載入... }
else if (r == FR_NO_FILE || r == FR_NO_PATH) { create_file(); }
else                                         { /* 什麼都不做 */ }
```

**寧可這輪少一個功能，不要毀掉使用者的資料。** 讀不到就這次不提供收藏
（愛心顯示成不可按），下次開機再說。

一般化：**任何「找不到就自動建立」的程式碼，都要先確認「找不到」是
「真的沒有」而不是「這次沒讀到」。** 硬體不可靠的時候，這兩者會混在一起，
而自動建立是破壞性的。

### 20.12 「每次剛好 15 次」的寫入上限

> **結論在 20.13：換一張卡就 100/100，是那張 8GB 的卡。**
> 這一節保留當時的推論過程，因為中間我把結論翻過一次又翻回來，
> 那個來回本身是教訓。

使用者問了一句很關鍵的話：**「那為何我 SD 用電腦寫就沒事」**。
問得對，而且直接推翻了我前一節的結論 —— 20.10 我寫「是卡片」，太快了。
正確的說法只能是「**在這塊板子的 SDMMC 介面上寫會失敗，在電腦的讀卡機上
不會**」，中間差了整條實體路徑與整個驅動。

#### 量到的

用 `g_dbg_wrtest`（純寫入、不含任何最愛邏輯）掃過三種設定：

| 設定 | 成功 | 第一次失敗 |
|---|---|---|
| BSP 預設（CLKDIV=4）、同一個磁區 | 15 | 第 16 |
| **時脈減半**（CLKDIV=8）、同一個磁區 | 15 | 第 16 |
| 時脈減半、**散布 30 個不同磁區** | 15 | 第 16 |

**跟時脈無關、跟寫哪裡無關，每次都精準停在 15。**
這排除了訊號品質（那會隨速度變）與特定壞區（那會隨位址變）。
確定性這麼強的數字，指向的是**狀態累積**，不是耗損也不是雜訊。

#### 第一次失敗的現場（只凍結第一次，board-notes 16.12）

| | 值 |
|---|---|
| `BSP_SD_WriteBlocks` | -4（PERIPH_FAILURE）|
| `hsd.State`（呼叫前）| 1 = READY |
| `hsd.ErrorCode` | **0 —— HAL 完全沒記錄到錯誤** |
| `SDMMC1->STA` | **0x1000 = DPSMACT**（資料路徑狀態機還 active）|
| `DCTRL` / `DCOUNT` | 0x90 / 0（傳輸沒啟用、計數歸零）|
| 卡片狀態 | BUSY |

**先前每次讀到的都是第二次重試之後的殘骸**（State=READY、Error=0），
所以看起來像「什麼事都沒發生」。只留第一次才看得到 DPSMACT。

#### 試過但沒用的

- **降時脈**：15 -> 15，完全沒變（所以不是訊號品質）
- **散開磁區**：15 -> 15（所以不是同一位置被寫爛）
- **傳輸前用 `HAL_SD_Abort()` 清 DPSMACT**：偵測到卡住 1 次、
  **清除成功 0 次** —— Abort 救不回來，跟 16.8 的 JPEG 週邊一樣
- **每 8 次主動 `BSP_SD_DeInit` + `Init`**：`BSP_SD_Init` 在卡片卡住的狀態下
  **永遠不返回**（README 記過這條），只能靠看門狗收場 —— 這條路本身會掛

#### 目前的判斷

證據指向**主機端（SDMMC 週邊 + ST HAL + BSP）累積的狀態**，不是卡片：
同一張卡在電腦上寫沒事、讀取數萬次零錯誤、只有完整重新初始化才復原、
而且失敗時 HAL 自己都不知道出了什麼事。

但**根因還沒找到**，所以這裡不下結論。留下的是可重現的量測手段：
`g_dbg_wrtest`（寫入壓力）、`g_dbg_sdclkdiv`（改時脈）、`g_dbg_wrspread`
（散開磁區）、`g_dbg_cardreinit`（重新初始化）與 `g_f1_*`（第一次失敗的現場）。

下一個該試的方向（還沒做）：

- 換一張卡跑同一組測試 —— 如果新卡也是 15，那就百分之百是主機端
- 用 `HAL_SD_WriteBlocks_DMA` 或 IT 版本，避開輪詢版的資料路徑收尾
- 每次寫入之後主動讀一次 R1 狀態（CMD13）確認卡片真的離開 PRG 狀態
- 查 ST 的 errata 有沒有 SDMMC 資料路徑相關條目

#### 這一節的方法教訓

**使用者一句「可是在電腦上就沒事」直接推翻了我的結論。** 我當時已經做了
很乾淨的隔離測試（純寫入、同磁區、不含最愛邏輯），數字也很漂亮 ——
但那個測試**從頭到尾都在同一塊板子上**，所以它能排除的只有「我的邏輯」，
排除不了「這塊板子」。

**隔離測試只能排除它真正變動過的東西。** 我把「軟體 vs 卡片」當成
二選一，忘記中間還隔著一整個 SDMMC 控制器與驅動。

### 20.13 結案：換一張卡，同一塊板子，100/100

使用者拿了另一張 512GB 的卡（exFAT）來比對。**同一塊板子、同一份韌體、
同一段程式碼、同一個測試，只換卡片：**

| 卡片 | 成功 | 失敗 | 寫入磁區 | 錯誤 |
|---|---|---|---|---|
| 8GB FAT32 | **15** | 85 | 31 | DPSMACT 卡住、BSP -4 |
| **512GB exFAT** | **100** | **0** | 204 | **全部 0** |

最愛的完整功能測試在 512GB 上也是滿分：收藏 20 張、20 張全部對上播放順序、
20 張全部播出來、0 失敗，每張剛好 2 次寫入。

**所以是那張 8GB 的卡。** 主機端（SDMMC 週邊 + HAL + BSP + 這份程式）
可以連續小型寫入 100 次零錯誤。

#### 「可是在電腦上寫就沒事」怎麼解釋

電腦用的是 USB 讀卡機 —— **那是另一個主機**，時序、協定實作、供電都不同。
一張處在邊界的卡完全可能「這個主機過、那個主機不過」。
所以「PC 沒事」**不能**用來排除卡片，它只說明失敗需要這個特定組合。

這跟第十章那個 PSRAM 是同一種東西：同一顆晶片、同一份韌體，
一塊板子好一塊板子壞 —— 邊界問題本來就是「組合」的性質，不是
「零件本身好或壞」的性質。

#### 我在這題上翻了兩次，這才是要記的

1. 20.10 我下「是卡片」。當時的隔離測試（純寫入、同磁區、不含最愛邏輯）
   很乾淨，但**它從頭到尾都在同一塊板子上**，只能排除我的邏輯。
2. 使用者問「那為何電腦寫就沒事」，我在 20.12 翻成「**應該是主機端**」。
3. 換一張卡實測，翻回來：確實是卡片。

**第 2 步才是真正的錯誤。** 使用者的質疑是對的 —— 我的證據確實不足以
支持「是卡片」。但正確的更新是**把不確定性放大**（「我還分不出來」），
不是**翻到相反的結論**（「應該是主機端」）—— 後者同樣沒有證據，
只是換一個沒根據的方向。

推翻一個結論的證據，通常不會自動成為相反結論的證據。

#### 真正該做的那個實驗，其實一開始就很明顯

**換掉唯一能換的那個變因。** 板子只有一塊、驅動只有一份，但卡片可以換 ——
而我在「PC 對照」上繞了很久，那個對照同時換掉了主機與讀卡機，
什麼都證明不了。

第十章的做法也是這個：「同一份韌體換一塊板子就閃」→ 換板子驗證。
**做對照實驗時，先問「我到底換了幾個東西」。**

#### 順帶：exFAT 支援

為了讀那張 512GB 把 `FF_FS_EXFAT` 打開了（要 `FF_USE_LFN >= 1`）。
代價是 **+8KB**，而外部 Flash 有 128MB、目前用 1.5MB。大容量卡出廠幾乎都是
exFAT，關著就直接掛不起來 —— 這個開關本來就該是開的。

### 20.14 「在電腦上就沒事」—— 負載不同，比較就不成立

20.13 用換卡證明了是卡片，但使用者一直有個合理的疑問沒被回答：
**「那為何我 SD 用電腦寫就沒事」**。後來在 PC 上做了修復嘗試，順手把它問死了。

#### 修復流程（照片已確認是硬碟副本，所以可以破壞性測試）

1. 重新分割，用滿整張 7.32GB（原本只切了 5.49GB）
2. 格式化 FAT32、簇 32K（跟原本一樣，方便前後比較）
3. **把整張卡填滿** —— 目的是逼控制器碰過每一個區塊，有機會重新對應弱區塊

#### 第 3 步的結果

| | |
|---|---|
| 寫入 | 1,509,949,440 bytes（1.41 GB）|
| 耗時 | 188 秒（約 8.0 MB/s）|
| 然後 | **裝置從 USB 匯流排整個消失**（`指定的裝置不存在`）|
| 復原 | **實體拔插斷電之後才回來** |
| 事後 | `HealthStatus: Warning`、`OperationalStatus: Full Repair Needed` |
| `chkdsk`（唯讀）| **掛住十分鐘沒有任何輸出**，只好中止 |

對照同一張卡在同一台電腦上的讀取：4493 個檔案、3.0 GB、**0 錯誤**、23.4 MB/s。

#### 所以「電腦寫沒問題」是取樣不足

使用者在電腦上做過的寫入是：刪一個檔、建一個資料夾、`chkdsk /f` 修一下 ——
**全都是幾十個磁區以內的小動作**，從來沒有連續寫過幾 GB。

| 主機 | 寫入負載 | 結果 |
|---|---|---|
| 板子 SDMMC | 15 次小寫入 | 控制器進 BUSY 不再回應 |
| PC USB 讀卡機 | 連續寫入 188 秒 | **裝置從匯流排消失** |
| 兩者 | 讀取 | 完全正常 |

**兩個完全獨立的主機、兩種不同的介面，寫入都會死、讀取都完美。**
而且兩邊的復原方式一樣：斷電重新初始化。

#### 這一節真正的教訓

**「另一個環境沒事」只有在負載相當時才是有效的對照。**

使用者說「電腦寫沒事」是誠實的觀察，而我一開始把它當成有效的反證，
還因此在 20.12 把結論整個翻掉。但那個對照**同時換掉了主機、介面**
**與工作量** —— 三個變因，什麼都證明不了。

要讓它成為有效對照，只需要在 PC 上施加**同等級的寫入量**。那一步花了三分鐘，
而我在它之前繞了好幾個小時。

**下次聽到「可是在另一邊就正常」，先問「那邊做的事情跟這邊一樣多嗎」。**

#### 結案

這張卡的寫入壞了，讀取還好。可以當唯讀的相簿卡繼續用，但**不要再寫它** ——
而且既然它會在寫入中途掉線，任何一次意外的寫入都可能把 FAT 弄壞。
不值得信任。

### 20.15 怎麼在幾分鐘內判斷一張 SD 卡好不好

整個第二十章繞了那麼久，可以濃縮成一句話：

> **判斷一張卡好壞，唯一有效的方法是寫夠多。讀取正常完全不代表什麼，
> 小量寫入正常也完全不代表什麼。**

那張壞掉的 8GB 在 PC 上：讀 4493 個檔案 3.0GB **零錯誤**、23.4 MB/s，
刪檔、建資料夾、`chkdsk /f` 全都正常。使用者因此一直認為「電腦寫沒問題」——
而連續寫 188 秒之後它就從 USB 匯流排消失了。

#### 方法一：PC 上（`tools/sd-check.ps1`）

```powershell
.	ools\sd-check.ps1 -DriveLetter F              # 預設寫 2GB
.	ools\sd-check.ps1 -DriveLetter F -FillAll     # 用光可用空間，順便驗容量真偽
```

不需要管理員權限，**不動既有資料**（只在可用空間建暫存檔，測完刪掉）。
做三件事：

1. 寫入指定的量（每 4MB 區塊開頭蓋上編號，抓得到「寫到別的地方去」）
2. **檢查裝置還在不在** —— 壞卡最明確的徵兆就是寫一寫整個從系統消失
3. 讀回來逐位元組比對

**預設 2GB 是有理由的**：那張壞卡死在 1.41GB，所以 2GB 是能抓到它的最小值。
想更保險就把 `-SizeMB` 調大，或直接 `-FillAll`。

#### 方法二：板子上（相簿韌體）

```
SWD 寫 g_dbg_wrtest = 100，然後讀 g_fav_st_ok / g_fav_st_fail / g_fav_st_first
```

**這個比 PC 那個敏感得多**：壞卡在 15 次小寫入就現形，而 PC 要寫到 1.41GB。
因為它走的是 SDMMC 直連，時序餘裕比 USB 讀卡機緊。

#### 判讀

| 現象 | 意義 |
|---|---|
| 全部通過 | 寫入可靠 |
| **裝置寫到一半消失** | **卡片壞了**，最明確的一種，要拔插斷電才回來 |
| 寫入速度 < 5 MB/s | 可疑（正常的卡至少十幾 MB/s）|
| 讀回來對不上，而且從某個位移開始 | 假容量卡，或那一段是壞區塊 |
| `OperationalStatus: Full Repair Needed` | 檔案系統已經被寫壞了 |

#### 實測數字（留著當基準）

| 卡片 | 板子 `g_dbg_wrtest=100` | PC 連續寫入 | PC 讀取 |
|---|---|---|---|
| 8GB FAT32（壞）| **15 成功 / 85 失敗** | **1.41GB / 188 秒後裝置消失** | 4493 檔 0 錯誤 23.4 MB/s |
| 512GB exFAT（好）| **100 / 0** | — | — |

#### 為什麼不用 chkdsk 判斷

`chkdsk` 檢查的是**檔案系統的一致性**，不是**媒體的寫入能力**。那張壞卡
`chkdsk` 跑得過（只是撈出 19MB 遺失簇），照樣是壞的。

而且卡片真的壞掉之後 `chkdsk` 連唯讀掃描都會掛住 —— 實測十分鐘沒有任何輸出。

#### 讀回來比對，一定要繞過作業系統的快取

第一版 `sd-check.ps1` 有個會讓整個工具失去意義的 bug，而且**它自己報「通過」**。

抓到它靠的是一個對不上的數字：

| | 讀取 3GB 的速度 |
|---|---|
| 驗證階段回報 | **139.8 MB/s** |
| 這張卡的實際讀取速度 | **23 MB/s** |

一張讀 23 MB/s 的卡不可能跑出 139.8 —— **那 3GB 全部命中作業系統的快取，
一個位元組都沒有從卡片讀回來。** 寫進去的資料還在 RAM 裡，比對當然全過。

後果：**假容量卡會直接騙過這個測試**（它的特徵正是「寫得進去、但讀回來不對」，
而我們根本沒讀到卡）。壞區塊同理。

解法是用 `FILE_FLAG_NO_BUFFERING` 開檔。.NET 的 `FileStream` 沒有安全的方式
設這個旗標（緩衝區位址必須對齊磁區，而 `byte[]` 不保證），所以直接 P/Invoke
`CreateFileW`，並用 `AllocHGlobal` 多配一頁自己對齊到 4096。

修好之後同一張卡同一個腳本：**139.8 -> 30.3 MB/s**，這才是卡片的真實速度。

**一般化：任何「寫進去再讀回來比對」的驗證，都要先問「這次讀取真的碰到
媒體了嗎」。** 中間只要有一層快取，驗證就退化成「檢查 RAM 還記不記得
剛才寫了什麼」。判斷方法很簡單 —— **看吞吐量合不合理**，快得不像話就是沒讀到。

這跟 16.11 那個「DMA2D 轉色快了四千倍」是同一類：**效能數字好得不合理時，
先確認那段程式真的執行了。**

#### 診斷工具自己也要驗證失敗路徑

`sd-check.ps1` 的「通過」路徑很容易測，**「失敗」路徑才是重點** ——
一個診斷工具最糟的失敗方式，就是**壞的東西它報通過**。那種錯要等到你
未來拿它去判斷一張真的壞卡時才會發現，而那時你會相信它。

偏偏那張壞卡是間歇性的：同一個腳本，一次寫 3GB 全過、一次 1.41GB 就掛，
想等它自己壞給你看並不可靠。

做法是**在寫入中途把卡實體拔掉**。它產生的例外訊息跟卡片自己掉線時
**完全一樣**（`指定的裝置不存在`），所以是忠實的模擬，而且三十秒就做完。

驗證結果：兩個失敗原因都被抓到、跳過讀取比對的分支正確、結束碼 1。

**能用外力製造出同樣症狀的失敗，就不要等它自然發生。**

### 20.16 直式影片要轉 ccw，而且要做進預設值

轉檔工具原本 `--rotate auto` 對直式來源選 `cw`。兩個方向都能把 720x1280
填滿 800x480，看起來都「對」—— 但只有一個跟板子實際的擺放對得上：
**cw 轉出來跟相簿直立模式差 180 度，放上去是上下顛倒的。**

這件事光看轉檔結果看不出來（PNG 打開是橫的，方向感覺不出錯），
要放到板子上才會發現。使用者手上能正常播的那部 `VIDEO.BIN` 就是 ccw 轉的。

**修正的方式是改預設值，不是寫在文件裡。** 一個「預設會產生錯誤結果、
但文件有說要改參數」的工具，等於把已知的正確答案外包給使用者的記性 ——
而下次用它的人（包括我自己）不會先讀文件。

這跟 18.3 那條是同一個家族：**畫面上寫著功能、程式裡沒有做**，
差別只在這次是「文件裡寫著正確用法、預設值卻是錯的」。

**橫向來源則是不轉**（`none`）。兩種來源的 `auto` 都已經在板子上實際放過
確認正確 —— 直式 ccw、橫向 none。

兩種填滿之後畫面的「上」指向面板的不同邊，所以看直式片與橫向片時相框要
轉 90 度。這是面板只有 800x480、而兩種長寬比相反造成的，沒辦法兩全。

順帶：這也是 `--preview` 存在的理由 —— 轉一部五分鐘的片要好幾分鐘、
輸出 176MB，方向錯了整包重來。先抓三張 800x480 的 PNG 看幾秒就好。
---

## 二十二、隨身碟模式：兩個 app 並存，以及四個都會讓它完全不動的根因

把板子的 SD 卡直接掛給電腦。外部 Flash 放兩個 app（`0x70000000` 相簿、
`0x71000000` ST 的 MSC_Standalone），相簿量 CN18 的 VBUS 決定要跑哪一個。

這一章的重點不是做法，是**四個各自獨立、少任何一個就整個不動的根因**。
每一個的症狀都極度誤導，而且它們是疊在一起出現的。

### 22.1 硬體：接 USB1（CN18），而且這塊板子沒有 JP2

USB1 = CN18 = 高速 480 Mbps；USB2 = CN17 = 全速 12 Mbps，差二十倍。
底部兩顆 USB-C 的絲印直接標著編號。

**ST 的 MSC readme 寫「for USB_HS: JP2」是錯的**，從別塊板子抄來的。
UM3289 列出的跳線只有 JP1（電源選擇）／JP3（NRST）／JP5（IDD）／JP6（MDC）。
照著找會一直懷疑是自己眼睛有問題。

JP1 留在 STLK：板子吃 CN7 的 5V，那個孔插充電器就行、不必是電腦。
**JP1 不能改到 USB1** —— 那樣拔線就斷電，相簿也不會跑。

### 22.2 HSE：app 在跑、中斷正常、SD 讀得到，就是不列舉

`MSC_Standalone` **沒有自己的 `SystemClock_Config`**，時脈完全繼承 bootloader。
它原廠的 bootloader 走 HSE，而相簿的只開 HSI —— 但 USB HS 的 PHY 需要
`RCC_USBPHYCCLKSOURCE_HSE`。

症狀完全不指向時脈：`VTOR` 讀出來是新位址、`uwTick` 正常遞增（中斷活著）、
SD 卡也讀得到，電腦就是看不到裝置。要讀 `RCC->CR` 的 bit16/17 才看得出來。

解法是在相簿 bootloader 的 `SystemClock_Config` **補開 HSE**，PLL 來源仍是
HSI，所以相簿的時序完全不變。從 app 那邊事後補開沒有用（PLL 已經在跑，
HAL 會拒絕，而且回傳值很容易被 `(void)` 吃掉）。

附帶發現：兩顆 bootloader 的 PLL **輸出頻率其實一樣**（600 / 400 MHz），
只是參考來源不同 —— 所以只補 HSE 就夠，不必動整個時脈樹。

### 22.3 快取一致性：MBR 讀得到，檔案系統掛不上

`usbd_storage_if.c` 的 SD 讀寫**完全沒有快取維護**，而 `BSP_SD_*Blocks_DMA`
是 DMA 進可快取的緩衝。

症狀同樣誤導：磁碟正確列舉、容量正確（461.2 GB）、**連 MBR 分割表都讀得
出來** —— 因為第一次讀的時候快取是冷的。之後每一次讀 CPU 都拿到殘留的快取
內容，所以檔案系統掛不上。

讀後 `SCB_InvalidateDCache()`、寫前 `SCB_CleanDCache()` 就好了。
用整體操作而不是 by_addr：`bot_data` 只保證 4 位元組對齊，by_addr 對未對齊
的範圍會連帶動到相鄰的快取行。

### 22.4 拔線事件：Disconnect 從不觸發，Suspend 一次開機來兩種

拔掉 USB 線之後**主迴圈會被 PD 堆疊卡住** —— 實測迴圈計數完全不動，
但 `uwTick` 照跳。所以任何放在主迴圈的偵測都不會執行。
放 SysTick 裡讀 ADC 也沒用：斷線後 ADC 不再轉換。
**只有 OTG 中斷這條路在斷線後還活著。**

在同一次開機裡記錄所有 PCD 回呼的觸發次數與時間：

| 回呼 | 次數 | 說明 |
|---|---|---|
| `Disconnect` | **0** | 拔線根本不觸發，這條路是死的 |
| `Reset` | 1 | 列舉時的匯流排重置 |
| `Suspend` | **2** | 開機後 182ms（列舉期）與 70812ms（拔線） |

**同一支回呼會收到兩種完全不同的事件。** 直接在 `Suspend` 裡重置的話，
app 一啟動就被列舉期那次打掉，電腦連磁碟機代號都出不來。
用時間門檻（「開機十秒後才算數」）是脆的。正確的閘門是
**「主機真的讀過資料」**（`STORAGE_Read_HS` 被呼叫過）—— 那是列舉完成、
磁碟已掛上的證據。

### 22.5 兩個 app 之間傳暗號：**不要放在別人的堆疊底下**

這一個是真兇，也是最難查的：相簿把「要切換」的暗號寫在 AXI SRAM 的
`0x24071BF0`，而 **bootloader 的堆疊指標是 `0x24072000`**，往下長只隔
1040 個位元組。bootloader 跑外部記憶體初始化就把暗號踩掉了。

症狀是**時好時壞**：拔線後有時回得來有時不行、按 reset 有時有效有時無效，
取決於 bootloader 那一次剛好在那個字上寫了什麼。

**這個可以完全自己驗，不需要任何人插拔**：寫一個特徵值進去、重置、讀回來。

| 位址 | 寫入 | 重置後 |
|---|---|---|
| `0x24071BF0`（原本） | `A5A5A5A5` | `00000000` 被踩掉 |
| `0x24050000` | `A5A5A5A5` | `AB0295A2` 也被踩掉 |
| **DTCM `0x20004000`** | `A5A5A5A5` | **`A5A5A5A5`** 完好 |

改放 **DTCM**：bootloader 的堆疊在 AXI SRAM，完全不碰 DTCM；相簿的 DTCM
只用 4KB、隨身碟 app 的堆疊從 `0x20010000` 往下，中間沒人動。

**而且 DTCM 不經過快取** —— 所以先前為了防「暗號的寫入卡在 D-Cache、
被隨身碟 app 的 `SCB_InvalidateDCache()` 整體失效丟掉」而加的那些
`SCB_CleanDCache_by_Addr` 全部不需要了。換個位置，一整類問題消失，
程式反而更簡單。

### 22.6 速度：瓶頸不是 USB

`MSC_MEDIA_PACKET` 預設 **512**（一個磁區），等於每個磁區都付一次
「USB 收 → 呼叫 SD → 忙等 `SD_TRANSFER_OK`」的完整來回。

| 緩衝 | 讀取 |
|---|---|
| 512 bytes | 1.74 MB/s |
| **32 KB** | **4.60 MB/s** |
| 64 KB | 4.59 MB/s（沒有再進步） |

取 32KB。**仍然比讀卡機（約 20 MB/s）慢五倍**，但瓶頸不是 USB —— 高速 USB
跑得到 40 MB/s，只用到十分之一。真正的瓶頸是 SD 與 USB 嚴格串列、沒有重疊，
要再快得做雙緩衝。

**量速度一定要先清作業系統快取**（重置板子讓磁碟區重新掛載），
否則會量到 1500 MB/s 的假數字 —— 跟 20.15 同一個坑，這輪又踩了一次。

### 22.7 照片格式：硬體 JPEG 只吃 baseline

不是這個功能的問題，但使用者透過隨身碟丟照片進去之後第一個踩到的：
**progressive JPEG 解不開**，而症狀是「畫面看起來像卡住」—— 相簿在幾十毫秒
內一張張失敗，螢幕上沒有東西可換。副檔名一樣是 `.jpg`、電腦上也打得開，
從外觀完全看不出來。實例：66 張裡有 64 張是 progressive。

還有檔案 2MB 上限（手機照片很容易超過）、寬度 3413、像素總量 3.4MP、
不支援 CMYK。`projects/album/tools/checkpic.py` 會一次檢查完，
`--fix` 能就地修好 progressive／檔案過大／尺寸過大三種。
